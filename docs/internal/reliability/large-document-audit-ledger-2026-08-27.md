# Large-document audit ledger

Date: 2026-08-27

Audit baseline: `0e24ed9c4cd589da32defd545551088dd29e2553`

No audit item below is closed solely by baseline evidence. The original
large-PDF annotation-save repair is published on remote `main` at
`915cc658ddae4d8cda0e58bbbafd7ef39528a5a9`. A later same-renderer remount
regression is corrected on remote `main` at
`0a8c63289a78ab1295f144a03697628ecd3ad438`. The ledger rows closed by these
repairs name the source commit, final verification commit, and matching gates.

Reconciliation on 2026-08-30 began with `961185b18` and was extended on
remote `main` through `a796feb5f`, `0b813bbea`, `56c561f70`, and
`89c05f7e7`. The published follow-up commits are `925872307` (imported
FreeText lifecycle and high-page print refusal), `8307de618` (prepared-witness
rollback recovery), `2e8ba691d` (native aggregate and nested stroke admission),
`3514f2e9a` (typecheck-only regression assertion), `574e9644c`
(bounded multipage TIFF staging and cancellation), `48b05406a`
(retired editor-reference projection), `0064bf875` (legacy live-editor
deletion fallback), `d9aa75cf0` (streamed browser DjVu TIFF decoding),
`9468c36b3` (bounded streaming PDF page trees), `2fb34608f` (retired Ink
reference projection), `58498c412` (lossless compact-source budgeting and
bounded placement-anchor projection), `0f2326e19` (protocol-handshake
cancellation regression), `01e7e6b9c` (bounded path-print planning),
`c5efe2fa1` (renderer-heartbeat and native-save phase correlation),
`b5ba82f8a` (stable eager-copy source admission), `416b43968` (early bounded
image-export refusal), `82097f977` and `45120f0e1` (content-fingerprinted
source witnesses), `160395caf` and `eb9ab83ea` (image-placement lease
release and shared-lease ownership), and `0ef7011d2` (hidden macOS
automation GPU guard). These commits were validated locally before
publication. Hosted CI is tracked separately and is not required to record a
source fix as published.

The same reconciliation later published `230ba5205` (lazy page-source
projection), `35702776b` (imported comment admission), `c27e86aa0` (exact
Text/Popup acceptance), `0f180fdcf` (retired annotation recreation),
`89432c542` (quiet-host exact-fixture ledger evidence), `b6df6e832` (stale
atomic-replace backup cleanup), `df72592c0` (persisted FreeText sidebar-delete
acceptance), `1b36b4f0b` (persisted editor-owned FreeText admission),
`f820922d7` (retired markup override filtering), `17d61287e` (macOS print
window readiness ordering), and `acf4c1505` (registered PDF.js
editor-constructor caching for xlarge annotation activation). All are
published on remote `main`.

`d0ab3779c` bounds save-time editor page scans, and `45ed63210` restores the
all-stage xlarge heartbeat assertion after the quiet-host rerun passed. Both
are published on remote `main`.

The next published follow-ups are `1d4034d16` (persisted sticky-note
geometry), `0d531209d` (its ledger reconciliation), `b08951d91` (continuation
chunk identity for placed images without stable keys), and `8b2095da3`
(earlier long-task probe coverage for xlarge FreeText placement), and
`35cb76aff` (symlink-destination policy coverage for atomic replacement).
The later `aabbc5191` test alignment keeps shape projections keyed by their
native stable keys and does not change the #144 acceptance status.

Post-audit follow-ups are tracked with their owning issues even when they do
not add a new baseline row. Issue #142 is verified and closed against
`0ef7011d2`: the hidden-macOS automation GPU guard and bounded renderer
readiness recovery passed the focused launch suite and a hidden macOS startup
probe at `c7c3e5e61`. Issue #144 has the retired-Ink projection correction in
`2fb34608f` plus the retired-null replay correction in `56c561f70`; the
follow-up `f820922d7` drops the remaining stale markup override, and
`5b260ad9c` contains revision-triggered shape imports at the save frontier.
The strict Highlight and repeated Ink lifecycle targets now pass. Issue #146 is
verified and closed. `SAV-020` records journaled serialized persistence, the
bounded Windows source witness, destination-scoped backup cleanup, fenced crash
recovery, and real-filesystem symlink refusal. Issue #147 is verified and closed
against `85670d391`; its pre-save
renderer stall is tracked separately by #149. Issue #148 is verified and
closed against `42601baf0` after the cross-process heavy-gate contention probe.
Issue #139 has a verified exact persisted-FreeText sidebar-delete slice in
`df72592c0` and a verified Mac resize-transition visibility slice in
`4f89f9a11`, with the hard-reopen and five-editor follow-up in `029e84c4f`.
`73fdb3848` closes the remaining PDF.js `42R` versus `42R0` identity mismatch
that could duplicate one persisted FreeText in the canonical projection; its
focused annotation/save gate passed 159/159 and the repository typecheck passed.
The exact Mac 882-page save/layout-transition regression also passed 1/1 after
that fix, with qpdf checks and the hard restart retained in
`.devkit/analysis/gates/2026-08-30T13-22-27-170Z-20230-d585d4d6.ndjson`.
The broader close-transition and multi-existing-editor acceptance remains open.
Issue #145 has the published handoff ordering fixes in
`17d61287e` and `46cdef1b5`. The latter keeps the PDF-plugin window
compositor-visible but fully transparent before the native sheet opens, so the
plugin can paint without exposing the dark backing window. The 18-test print
unit suite, Electron/tests typechecks, ESLint, and diff check pass; real
system-sheet and nonblank-output acceptance remains open.
Issue #149 is verified and closed against `45ed63210` after the exact quiet-host
2646-page lane passed with the placement-stage heartbeat assertion restored.
Issue #123 is verified and closed against `9d016f200`. The exact 2.168 GB
fixture passed APFS clone and read/write reopen, stable-source fallback,
save and hard reopen, constrained-volume refusal, and macOS path-alias
cancellation and range-cache invalidation. The durable Mac evidence is in
`docs/internal/reliability/issue-123-macos-acceptance-2026-08-30.md`.

The reconciliation also includes `9ef3f01b0` (deprecated OCR DOCX
cancellation), `b4bafc271` (lazy sparse zoom-anchor projection), and
`b9a7bdc64` (fail-closed live FreeText deletion), all published on remote
`main`.

The hosted quality run `33276462757` is an ancestor of the current main tip,
not a current-head result. It reported 1,159 passing files and one skipped
file, with 10,077 passed tests and seven skipped tests, but Browser Integration
still failed. The older 1,158-file and 9,992/9,998-test counts retained in
row provenance below are historical gate records, not current-head evidence.
There is no hosted CI result for the current tip yet.

Issue #148 was opened after this audit for heavy-gate zombie-holder recovery.
The owner-death and cross-process capacity-1 probe passed against
`42601baf0`, and the issue is closed. The baseline rows below retain their
original provenance; this post-audit result is recorded here so it is not
mistaken for an open gate.

The four Markdown reports were read in full. The telemetry JSON was read only
for the fixture object and scalar or summary fields needed for fixture evidence.
Report references below are relative to
`.devkit/analysis/exhaustive-audit-20260827/`.

## Fixture identity

| Fixture | Role or artifact | Bytes | Pages | SHA-256 | Provenance and admission |
| --- | --- | ---: | ---: | --- | --- |
| Local artifact | Exact local Zaliznyak fixture admitted on the primary Mac | 722178517 | 882 | `1660bced91f628b9acbb2fc0f9dac29fe783a3f43d26231d8f3b0c73133b21b6` | Primary-checkout evidence; qpdf page count and `qpdf --check` passed; filesystem birth/modified around 2026-08-27 19:37 |
| Audited VPS 882-page artifact | Retired audit upload identified by hash | 722176299 | 882 | `4f5c6a438f19a0b19faff37882be6f0bc9199fbf6ba5d0694ab25d4d32ce897b` | Supplied VPS source; `stat`, qpdf page count, SHA-256, and source `qpdf --check` passed |
| Xlarge artifact | Retired audit upload identified by hash | 2168527413 | 2646 | `5609c151c1cec881da4b97ec7028250574f8f0ee67540dcdc8808cc7b8ab0aea` | Supplied VPS source; source `stat`, qpdf page count, SHA-256, and `qpdf --check` passed; telemetry staged the same byte count and page count |

Fixture evidence is in `full-audit.md:124-130`, `linux-flow-audit.md:16,43-46,95-101`,
and `xlarge-followup-telemetry.json:2-12`. The xlarge scalar summary records a
67,025.1 ms Session B save phase at `xlarge-followup-telemetry.json:68-70`, a
3,350 ms maximum heartbeat gap in the report at `linux-flow-audit.md:132-143`,
and a Session B renderer heap delta of 108,776,424 bytes at
`linux-flow-audit.md:143`.

The local artifact is not equivalent to the audited VPS artifact even though
the document name matches. The local file is 722178517 bytes with SHA-256
`1660bced91f628b9acbb2fc0f9dac29fe783a3f43d26231d8f3b0c73133b21b6`; the VPS
file is 722176299 bytes with SHA-256
`4f5c6a438f19a0b19faff37882be6f0bc9199fbf6ba5d0694ab25d4d32ce897b`.

## Ledger fields and deduplication

Each entry includes a stable ID, severity, classification, finding, exact
evidence, the required red regression test or probe, owner/status, fix SHA,
gates, and remaining acceptance. `parent-triage` means the parent task must
assign and sequence the work.

Distinct finding count: **105**.

Issue #134 acceptance is complete. `63211b435` widened the slow exact-fixture
pointer readiness window, `9d9ffb44c` kept the native save on one staged copy,
and `85670d391` scoped the heartbeat policy to the save window. The quiet-host
882-page lane passed 3/3 with all six native save totals below the 8000 ms SLO;
the quiet-host 2646-page lane passed 2/2, with the 5000 ms policy passing in the
actual save window. Both lanes required visible controls, live PDF.js dirty
state, committed revisions, qpdf checks, hard restarts, and bounded structural
probes. The issue is closed; the separate pre-save placement stall remains
open under #149.

Issue #137 is verified and closed against published `9fcc32c34`. The
RGB-camera preview result was a false positive in the old relative comparison
of near-zero variance proxies. The oracle now compares the absolute variance
residual against measured stroke width and keeps a regression for real
two-word divergence. The current export, stroke-weight, and word-loss checks
pass, as does the focused harness suite (2 tests). The historical SCAN rows
below retain their original baseline-red provenance; it is no longer a current
main failure.

Ambiguous deduplication decisions:

- `SAV-008` combines the full-audit original-path race with the native report's
  compare-and-swap wording because they describe one JavaScript publication
  race. `SAV-011` remains separate because `AtomicOutput` also has symlink
  replacement behavior and a separate native helper boundary.
- `SAV-009` combines the full-audit and native eager-copy findings. `SAV-019`
  combines their protocol-handshake findings. `CAP-001`, `CAP-002`, and
  `CAP-003` retain separate cap rows even where individual selection or
  annotation rows describe their user-visible symptoms.
- `TEST-014` combines the repeated fixture-hash admission gap. `TEST-021`
  through `TEST-023` remain separate because committed-event observation, a
  post-save process restart, and a second save are separate acceptance gates.
- Repeated unproven behaviors are represented once in `TEST-024` through
  `TEST-027` and again only as checklist wording where the reports require the
  same gate. Rejected hypotheses and confirmed bounded paths are listed after
  the ledger and are not counted as findings.

Rows remain open unless they name a fix SHA and matching acceptance evidence.
The original audit reports contain no implementation SHA that proves a fix.

The published save series is `c50c0aaa9`, `f5fd24869`, `5ef250df6`,
`e1ff8e8a0`, `7cb46ad96`, `942b3b736`, `d52fcb0a0`, `82eeedfa0`, and
`915cc658d`. Final macOS session
`e2e-run-mtci2ep2-4d0947-large-pdf-1787894253889` passed the strict sticky-note
scenario at `915cc658d` in 124.139 s, with 158.49 s total runner time. It used
fresh Electron processes, visible pointer and keyboard input, live PDF.js
annotation storage, exact committed events, two hard restarts, clean hydration,
bounded structural probes, the runtime IPC probe, and strict teardown. Both
saves met the unchanged 8 s limit. The preserved second-save telemetry records
1,978.6 ms renderer total, 1,870.5 ms native IPC, 511.5 ms native apply, and
89.9 ms native commit.

Linux visible session `e2e-run-mtci4rdz-92df9d-large-pdf-1787894366683`
passed at the same commit with renderer saves of 3,843.3 ms and 4,681.5 ms,
native mutation phases of 2,929.1 ms and 2,940 ms, and staged commits of
278.4 ms and 163.4 ms. Linux hidden session
`e2e-run-mtciac46-c5fa9d-large-pdf-1787894626641` passed with renderer saves
of 3,243 ms and 5,259.3 ms, native mutation phases of 2,740.6 ms and 2,479.6
ms, and staged commits of 285.6 ms and 133.2 ms. The visible run recorded
`hasShownWindow:true`, live Electron and Xvfb process identities, `DISPLAY=:118`,
and a retained X11 screenshot. Both modes passed fixture admission, both hard
restarts, both native commits, Contents, NM, AP, Rect, Popup-parent, qpdf,
revision, target-path, runtime IPC, and survivor checks. Evidence is retained
under `.devkit/analysis/zaliznyak-save-linux-acceptance/final-915cc658d/`.

Final gates on `915cc658d` passed: typecheck, lint, 9,660 unit tests across
1,136 files with one skip, the full native workspace tests, strict native
clippy, native formatting, focused save and working-copy suites, and diff
checks. CodeRabbit ran twice. Its save-scope findings were fixed or rejected
with evidence, and the final pass found no unresolved defect in this repair.
Independent specification and standards reviews passed after the duplicate
timing wrapper was removed.

### 2026-08-28 same-renderer remount correction

A later manual recording showed that a successful native save could remount
the live PDF source, reset the user to page 1, and discard the in-memory note
session even though the committed PDF was structurally valid. The earlier
strict lane proved native persistence and restart durability, but it did not
fail on this immediate same-renderer continuity regression. Renderer logs
separated the first pre-write dirty-baseline refusal from the later successful
native mutation and staged commit; the SSD was not the bottleneck.

Commit `0a8c63289a78ab1295f144a03697628ecd3ad438` makes the explicit
`preserveLoadedSource` save contract take precedence for native path-backed
documents and records the reload revision without replacing the live source
object. Native-path history now prefers the reload source. The exact acceptance
test also requires the same renderer to stay on page 16 with its visible note
button and editor before it proceeds to the two hard-restart sequence.

The final hidden macOS session
`e2e-run-mtcpxkx6-4dcfb4-large-pdf-1787907465894` passed the selected test in
144.462 s, 181.37 s total. The final sandboxed hidden Linux session
`e2e-run-mtcpy01g-f3be4e-large-pdf-1787907488727` passed in 154.120 s,
194.34 s total. Both admitted the exact 722178517-byte, 882-page fixture with
SHA-256 `1660bced91f628b9acbb2fc0f9dac29fe783a3f43d26231d8f3b0c73133b21b6`,
performed two native saves and two hard restarts, required exact committed
events and revisions, checked Contents, NM, AP, Rect, Popup parent and
`qpdf --check`, ran the runtime IPC probe, and left no task-owned process or
lease. Per the final user instruction, the current-commit reruns were hidden
only; earlier visible or no-sandbox runs are diagnostic evidence, not final
acceptance for this correction.

Final gates on `0a8c63289` passed: full typecheck, lint, 9,662 unit tests across
1,136 files with one skip, focused persistence 25/25, preload 18/18, exact
two-file OCR isolation 17/17, and diff checks. One first full-unit attempt hit
the known five-test OCR mock-order flake; the isolated pair passed immediately
and the complete rerun passed. CodeRabbit ran the allowed three passes. The
last finding required filtering hidden or stale annotation nodes; the final
test checks the visible Vue note button and textarea instead of the intentionally
hidden PDF.js persistence anchor. No fourth review pass was run after the
capped finding was fixed.

A second read-only Fable review acknowledged the verified phase telemetry
before inspecting the publication code. It found that Linux repeated a full
722 MB physical working-copy sync after native publication. It recommended a
clone-first immutable-source link fallback, limited to original-to-working-copy
sync, plus an exclusive-inode guard on the only same-path metadata appender.
`e1ff8e8a0` implements that design. Real-filesystem regressions prove inode
sharing, rollback to distinct old inodes, external-source expectation
invalidation, cross-device copy fallback, and refusal to append through a
shared inode. CodeRabbit's final review found no issue in the performance diff.

An earlier exact local-fixture run reproduced `SAV-001` after the real macOS
clone path was enabled in the E2E app. Session
`e2e-run-mtbvb63n-0e8893-large-pdf-1787856031691` reached the native staged
commit in 1.018 s, then failed closed. Instrumentation recorded
`missing original file expectation` in `originalPathSaveBaseMatches` and a
native staged commit result of `{applied:false,validation:null}`. This rules
out native mutation failure for that run and confirms that the normal
unencrypted clone path omitted the original-file witness required by commit.
The source fix removes deferred original-file expectations from the three
normal mapped working-copy creation routes. Its real-store regression and the
strict macOS, Linux visible-Xvfb, and Linux hidden acceptance cases are green
on `915cc658d`, which is an ancestor of remote `main`.

## Save and working-copy correctness

| ID | Severity | Class | Finding | Evidence | Required red regression test or probe | Owner/status | Fix SHA | Gates | Remaining acceptance |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SAV-001 | High | product | Normal clone or eager working-copy paths can omit the original-path expectation, so native save reports a false source mismatch. | `full-audit.md:15` cites `originalPathSaveBaseMatches.ts:19-22`, working-copy creation and materialization, native save handlers, and a 20-iteration loop failing on iteration 4. Exact local session `e2e-run-mtbvb63n-0e8893-large-pdf-1787856031691` independently reproduced the missing expectation after native staging completed. | Repeat the focused routes across normal clone and eager working-copy creation; assert native save accepts the matching original and working-copy revision. | fixed/published | `c50c0aaa9`; verified at `915cc658d` | Final typecheck, lint, 9,660-unit-test, native workspace, clippy, formatting, CodeRabbit, spec-review, and standards-review gates passed. Exact macOS session `e2e-run-mtci2ep2-4d0947-large-pdf-1787894253889` and Linux visible and hidden sessions `e2e-run-mtci4rdz-92df9d-large-pdf-1787894366683` and `e2e-run-mtciac46-c5fa9d-large-pdf-1787894626641` passed at `915cc658d`. | Complete. The verified commit is published on remote `main`. |
| SAV-002 | Critical | product | An original can be committed while sidecar or cleanup failure leaves the working copy, sidecar, or journal at a different revision. | `full-audit.md:16` cites `transitionOriginalAndWorkingCopyRevision.ts:46-92` and `documentRevisionStore.ts:318-331`; rollback does not restore sidecar or journal. | Inject a sidecar or cleanup failure after replacement; assert original bytes, working-copy bytes, sidecar, and journal all return to one revision. | issue-122 / fixed | `e4dac65c0` | Focused transaction fault tests and TS7 typecheck green; validate ran 1,158 files and 9,998 tests green with coverage ratchets green, then its idle lint coordinator was stopped; focused ESLint and duplication gate green; CodeRabbit CLI rate-limited, documented fail-open used without charges. | None. Durable sidecar publication is the commit point; pre-commit failure restores bytes and journals, while post-commit cleanup cannot reverse success. |
| SAV-003 | High | product | Transition copies into the working copy with `durable:false` before writing the durable sidecar, so a crash can split file bytes from the trusted revision token. | `full-audit.md:17`. | Stop the process between the non-durable copy and sidecar write; reopen and assert the file bytes and trusted revision token agree. | issue-122 / fixed | `e4dac65c0` | Focused crash-boundary tests and TS7 typecheck green; validate ran 1,158 files and 9,998 tests green with coverage ratchets green, then its idle lint coordinator was stopped; focused ESLint and duplication gate green; CodeRabbit CLI rate-limited, documented fail-open used without charges. | None. Working-copy bytes and directories are synced before the durable sidecar becomes visible. |
| SAV-004 | High | product | Save As can commit the target, then report failure when a later step rejects the result, while the renderer keeps the old original path. | `full-audit.md:18` cites `documentSave.service.ts:150-160,251-268`, `serializedPdfPersistence.ts:647-706`, and `createDocumentPersistence.ts:1103-1113,1167-1173`. | Force the post-commit rejection; assert the returned result, renderer path, target bytes, and subsequent save all describe the same target. | issue-122 / fixed | `f56482f43`, `5d1bb5ea2` | Focused Save As and renderer tests and TS7 typecheck green; validate ran 1,158 files and 9,998 tests green with coverage ratchets green, then its idle lint coordinator was stopped; focused ESLint and duplication gate green; CodeRabbit CLI rate-limited, documented fail-open used without charges. | None. A committed target path is returned and adopted even when working-copy refresh needs repair. |
| SAV-005 | High | product | The native mutation handler captures the original path before queue entry, so a queued request can use a path remapped by a preceding Save As. | `full-audit.md:19` cites `nativePdfMutationSaveHandlers.ts:287-295,472-480`, `workingCopySave.ts:258-270,298-307,437-450`, and `documentSave.service.ts:131-157`. | Queue a native mutation, perform Save As before it runs, then assert the mutation writes only to the remapped target and its receipt. | issue-122 / fixed | `f56482f43`, `5d1bb5ea2` | Focused ordinary, staged-native, and streamed queue tests and TS7 typecheck green; validate ran 1,158 files and 9,998 tests green with coverage ratchets green, then its idle lint coordinator was stopped; focused ESLint and duplication gate green; CodeRabbit CLI rate-limited, documented fail-open used without charges. | None. Queued saves resolve the mapping after admission, and streamed artifacts rehome beside a remapped target before publication. |
| SAV-006 | Medium | product | Rust incremental copy-on-write cloning can inherit mode `0444`, fail to reopen read/write, and abort without its streaming fallback. | `full-audit.md:20` cites `native/evb-native-support/src/output.rs:103-142` and `native/pdf-page-ops/src/incremental.rs:169-189`. | Create a read-only clone destination and run incremental save; assert the tested streaming fallback completes or returns a typed refusal without aborting. | issue-123 / fixed/published | `89d80a06a` plus inherited native fix `f2f6a2edf`; Mac acceptance `9d016f200` | Linux forced-unsupported clone, streaming fallback, and mode-0555 refusal passed. On macOS, APFS cloned the exact 2,168,527,413-byte fixture in 29 ms, reopened it with `O_RDWR`, matched SHA-256, and passed `qpdf --check` with 2,646 pages. Focused Mac working-copy and file-access coverage passed 6 files/154 tests. Evidence: `docs/internal/reliability/issue-123-linux-acceptance-2026-08-29.md` and `docs/internal/reliability/issue-123-macos-acceptance-2026-08-30.md`. | Complete across the tested Linux fallback/read-only paths and macOS APFS clone/reopen path. |
| SAV-007 | High | product | The second save in the audited exact-file interaction chose the serialized fallback and was rejected as `native-save-required`. | `full-audit.md:21`. The strict acceptance proves the actual restored sticky-note case, rather than ordinary clean FreeText hydration. | In a fresh process, save an annotation, hard-restart Electron, reopen the committed file, make a second annotation edit, and assert the second save uses the native path and commits. | fixed/published | `f5fd24869`; verified at `915cc658d`; same-renderer remount correction `0a8c63289` | The final hidden macOS and sandboxed hidden Linux sessions at `0a8c63289` preserved the live page and note session, completed the second native save after restart, then restarted again and verified both commits. Full gates are recorded above. | Complete. The corrected commit is published on remote `main`. |
| SAV-008 | High | product | Original-path save was a check-then-rename race. An external editor or sync client could change the original after the base check and before unconditional replacement. | Fixed in `originalPathSaveBaseMatches.ts`, `transitionOriginalAndWorkingCopyRevision.ts`, `atomicReplace.ts`, and every original-path publisher. The regression in `transitionOriginalAndWorkingCopyRevision.test.ts` pauses publication, replaces the original from a second process with same-size content, and proves the external bytes survive with a typed conflict result. | Pause after save admission, replace the original atomically from a second process with same-size content, release publication, and assert the external bytes remain and the save returns a conflict. | save-reliability / fixed and published | `b1a7f5b66` | `node scripts/run-all-gates.mjs --only validate` passed full lint, type coverage, 1,146 files and 9,822 tests, the native workspace, resource matrix, strict build, and bundle integrity; its blocking smoke timed out because the validation plan omits `EVB_PDF_PAGE_OPS_ENABLE=1`. The same smoke wrapper passed 3 files and 7 tests with that required flag. The final focused run passed 8 files and 92 tests; Electron and test TypeScript checks, ESLint, Fallow, and two CodeRabbit passes also passed. | Complete for JavaScript original-path save flows. A held file handle records device, inode, size, nanosecond timestamps, link count, and bounded content samples, then rechecks path identity at the final publication seam. `SAV-011` and #122 retain native `AtomicOutput`, symlink policy, and wider transaction work. |
| SAV-009 | Medium | product | Eager `copyFile` fallback can create a mixed working-copy snapshot while the source changes, without post-copy handle, stat, or hash proof. | `full-audit.md:93`; `native-crosscut-audit.md:56-88` cites `workingCopyCreation.ts:112-153` and contrasts the safer lazy path. | Force an unsupported clone, replace the source from a second process during copy, and assert the working copy matches one complete source revision. | issue-123 / fixed/published | `89d80a06a`, `b5ba82f8a`, Mac acceptance `9d016f200` | Linux held-source replacement and exact fallback passed with matching SHA. The published eager-copy fix holds one admitted source revision. On macOS, the exact fixture stable-source copy matched 2,168,527,413 bytes and SHA-256 in 2.25 seconds; the xlarge Electron run passed save, `qpdf --check`, fresh-renderer reopen, annotation-object comparison, structural comparison, and cleanup in 294.3 seconds. Evidence: the Linux and Mac issue-123 acceptance reports. | Complete. Source replacement cannot publish a mixed snapshot, and the exact fixture passed copy, save, and fresh-renderer reopen on macOS. |
| SAV-010 | High | product | Native incremental save can parse one source state with qpdf, then copy a newer source before append. | `full-audit.md:94`. | Change the source between qpdf structural admission and the copy; assert the append aborts or re-admits the same source revision. | issue-122 / fixed | `e4366a47a` | Focused native packages green; `cargo fmt --check` and clippy green; release workspace green after serial retry of seven multi-GiB tests following concurrent `ENOSPC`; CodeRabbit CLI rate-limited, documented fail-open used without charges. | None. Qpdf admission, source staging, and append share one held revision witness. |
| SAV-011 | High | product | `AtomicOutput` checks metadata and renames later, sharing the external-edit race; replacing a symlink also changes the link into a regular file. | `full-audit.md:98`. | Exercise external replacement between witness and rename, then exercise a symlink destination; assert external bytes survive and link semantics follow the intended contract. | issue-122 / fixed | `f2f6a2edf` | Focused atomic-output tests green; `cargo fmt --check` and clippy green; release workspace green after serial retry of seven multi-GiB tests following concurrent `ENOSPC`; CodeRabbit CLI rate-limited, documented fail-open used without charges. | None. Publication uses held-descriptor compare-and-swap; destination leaf symlinks are rejected and ancestor symlinks remain allowed. |
| SAV-020 | Medium | product | Serialized `working_copy` persistence replaced the PDF and advanced its revision in separate operations, so a crash could leave new bytes under the old revision token. | Issue #146 lower item. | Fail between working-copy replacement and revision publication; recovery must restore one prior content/revision pair, and successful persistence must publish the revision only with the new bytes. | issue-146 / fixed/published | `a59d42a2e`, `82097f977`, `45120f0e1`, `0b813bbea`, `2e9c1efd1`, `c6c5fd44b` | Published journaled serialized-working-copy persistence and source-witness fixes. The witness fingerprints full content on Windows when size and metadata can be restored, then rechecks device/inode/size/mtime/ctime after hashing. Rollback refreshes the durable expectation after original restore. `2e9c1efd1` adds bounded small-file fingerprint coverage, a stat-only path above 64 MiB, and fail-closed source-mutation coverage. `c6c5fd44b` adds real-filesystem regressions proving crash recovery preserves an external replacement and retains its backup and journal, while `atomicReplace` refuses a symlink leaf without altering the link, referent, or source. Focused transaction coverage passed 7 files/56 tests; TypeScript, ESLint, diff check, and CodeRabbit passed. | Complete for the selected transaction policy. Journaled persistence, rollback and recovery fencing, symlink refusal, bounded Windows content fingerprinting, and stale backup cleanup are implemented. The real-filesystem recovery and symlink probes ran on POSIX. Native Windows-host execution was not performed on this Mac. |
| SAV-012 | Medium | product | Reflink fallback can perform a physical multi-gigabyte copy and fail with `ENOSPC` without a capacity-aware plan. | `full-audit.md:99`. | Constrain free space below the source size, force reflink fallback, and assert a bounded refusal before a partial working copy is registered. | issue-123 / fixed/published | `89d80a06a`; Mac acceptance `9d016f200` | Linux tmpfs admission and Mac APFS constrained-volume admission passed. The Mac probe exposed 1,563,877,376 available bytes for the 2,168,527,413-byte source, forced clone fallback, and received typed `WORKING_COPY_MATERIALIZATION_NO_SPACE` twice before target open. No target or materializing file remained; registration stayed retryable and returned to `lazy-original`. | Complete. Both Linux and macOS refuse the physical fallback before registration when destination capacity is below the exact source size. |
| SAV-013 | Medium | process | Killing a save utility can kill its wrapper while leaving a qpdf child alive. | `full-audit.md:100`. | Start a save utility whose qpdf child blocks, kill the wrapper, and assert the child process group is gone before the operation settles. | issue-124 / complete | `badb9f554` | Real headless #135-style acceptance passed: `EVB_PDF_PAGE_OPS_ENABLE=1 bash scripts/test-electron-e2e-headless.sh --no-build e2e-save-pipeline tests/e2e/electron/savePipeline.e2e.test.ts -t 'leaves no process, handle, or partial write behind when the app is stopped hard mid-save'`; 1/1 test passed, including hard stop, descendant liveness, handle cleanup, staged artifact cleanup, unchanged source, and reopen. Evidence: `.devkit/issue-124/sav-013-acceptance.log`. | Timeout and forced-stop variants are represented by the hard-stop child-liveness assertion; no remaining acceptance. |
| SAV-014 | High | product | The mutation queue has no sender-lifecycle ownership, so save optimization and validation signals can be lost between stages. | `full-audit.md:101`. | Close or replace the sender at each queue stage; assert the operation fails closed or completes with a durable receipt and no lost signal. | issue-124 / complete | `dc90cbc3b` | `workingCopyMutationQueue.test.ts` 3/3, including queued-stage owner teardown; eslint on touched files; `pnpm run typecheck` passed. | None. The queue-stage owner-teardown regression is covered by the published lifecycle suite. |
| SAV-015 | Medium | process | Lazy materialization has no renderer teardown hook, so staged artifact leases can survive renderer death for five minutes. | `full-audit.md:102`. | Kill the renderer during materialization, assert lease release or bounded expiry, and check no stale staged handle remains usable. | issue-124 / fixed/published | `785590942`, `631ed2787` | Exact Mac 882-page acceptance in `tests/e2e/electron/issue124LifecycleAcceptance.e2e.test.ts` passed the renderer-crash materialization case; `mainOperationLifecycle.test.ts` and `workingCopyMaterialization.test.ts` passed 14 + 14 tests. The run validated no materializing temp, no working copy, and unchanged source bytes/hash. | Complete. The verified acceptance harness is published on remote main. |
| SAV-016 | Medium | process | Serialized completion can race sender cleanup and settle after the sender is gone. | `full-audit.md:103`. | End the sender at completion boundaries and assert one deterministic settlement with no late callback or unhandled rejection. | issue-124 / complete | `785590942` | `mainOperationLifecycle.test.ts` 13/13, including duplicate completion after teardown; eslint on touched files; `pnpm run typecheck` passed. | None. Duplicate completion after teardown is covered by `mainOperationLifecycle.test.ts`. |
| SAV-017 | Medium | product | Image-placement handles leak on cancel or failure. | `full-audit.md:103`. | Cancel and fail image placement at each native and renderer boundary; assert all handles are released and a retry starts cleanly. | issue-124 / fixed/published | `4a0314078`, `160395caf`, `eb9ab83ea`, `631ed2787` | Exact Mac 882-page acceptance passed cancel, induced native failure, fresh retry, and final handle-release checks. `managedTempFileHandles.test.ts` and `usePdfImagePlacement.test.ts` passed 74 + 11 tests, including shared-lease and retry coverage. | Complete. The verified acceptance harness is published on remote main. |
| SAV-018 | Medium | process | Selected-page qpdf work, optimization, and post-save validation ignore print or export cancellation. | `full-audit.md:104`. | Abort each operation during qpdf, optimization, and validation; assert child work stops and no output is reported committed. | issue-124 / fixed/published | `3b916f15`, `65965ad99`, `631ed2787` | Exact Mac 882-page acceptance passed the selected-page print teardown with a real qpdf child held at spawn, then verified process death, args-file removal, qpdf output-directory removal, print-temp invariants, and unchanged source/working-copy hashes. Supporting documents-print and native-tool/qpdf tests passed 74 + 53 tests; TypeScript, ESLint, and diff checks passed. | Complete. The verified acceptance harness is published on remote main. |
| SAV-019 | Low | process | Protocol-handshake cancellation rejects the caller while the `--protocol-version` child continues until timeout or natural exit. | `full-audit.md:103`; `native-crosscut-audit.md:90-116` cites `runNativeToolCommand.ts:49-56,84-109,117-152`. | Use a helper that sleeps on `--protocol-version`, abort the caller, and assert the helper PID or detached process group is gone before rejection. | issue-124 / complete | `e802f71ee`, `0f2326e19` | Real sleeping helper test passed in `protocolHandshakeCancellation.test.ts`; the published protocol-boundary regression passed and the helper PID was gone before caller rejection. Evidence: `.devkit/issue-124/sav-019-acceptance.json`. | No remaining acceptance. |

## PDF page metadata and annotation persistence

| ID | Severity | Class | Finding | Evidence | Required red regression test or probe | Owner/status | Fix SHA | Gates | Remaining acceptance |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| PDF-001 | Critical | product | Structural page operations can delete unopened outlines because the lazy bookmark state is empty when metadata is captured. | `full-audit.md:25` cites `PdfSidebar.vue:87-98,359-371`, `useBookmarkState.ts:19-42`, `usePageOperations.ts:159-175`, `pageOpsMainBindings.ts:156-169`, and `native/pdf-page-ops/src/catalog.rs:437-446`. | On a PDF with outlines, never open the bookmark panel, rotate/delete/reorder/crop/move/insert, save, reopen, and assert the outline tree is unchanged. | fixed/published | `07d396126`, `83e48fea0`, `6828e241a`, `519bca85d` | Six-operation fresh-process qpdf matrix passed; focused ESLint; changed-file TypeScript check; both scan-cleanup tests passed when rerun alone; `node scripts/run-all-gates.mjs --only validate` ran, with unrelated scan-cleanup host-load timeouts; CodeRabbit CLI rate-limited after included reviews | Complete. The verified fix is published on remote main. Test: `tests/e2e/electron/nativeSaveReopen.e2e.test.ts` preserves outlines and page labels through the six-operation fresh-process matrix. |
| PDF-002 | Critical | product | Structural page operations on documents over 200 pages can replace custom page labels with default numbering. | `full-audit.md:26`; `cap-audit.md:62-82` gives the 201-page probe and null-to-empty remap call graph. | Run the null-label remap unit case and a 201-page labeled PDF rotate/save/reopen test; assert every label range survives. | fixed/published | `de25dea4c`; ledger docs `6a1da805e`, `2c702f8aa` | Focused Vitest: 18 passed; expose and E2E topology Vitest: 44 passed; 201-page Electron matrix `keeps every compact label through mutations, save, and reopen`: passed; touched-file ESLint; aggregate validate passed typecheck, coverage, fallow, and Rust checks before host-stalled lint; CodeRabbit rate-limited, fail-open | Complete. The automated matrix covers rotate, delete, reorder, crop, move, insert, save, and fresh reopen. The reopened snapshot preserves all four compact ranges with `pageLabels` still null. |
| PDF-003 | High | product | Imported `/Text` annotations with Popup notes disappear from the canonical annotation model. | `full-audit.md:27` cites `buildPdfAnnotationCommentSummary.ts:78-91,159-222` and `annotationApplication.ts:228-229`; direct probe returned `hasNote:false` and zero entities. | Import a `/Text` annotation with a Popup, assert the canonical entity has a note, save without edits, reopen, and assert the entity remains. | pdf-import / fixed | `af4ecb65b`, `35702776b`; verified at `c27e86aa0` | Red-first import regression; shared subtype-aware canonical-store policy; focused sync suite 62/62; exact 882-page headless Electron import, clean-save, hard-restart, and qpdf object/link proof passed; repository typecheck, targeted ESLint, fallow, focused boundary check, and affected scan-cleanup oracles passed. | Complete. |
| PDF-004 | High | product | Moving an imported sticky note changes text and modification time but has no native geometry update, so reopen restores the old location. | `full-audit.md:28` cites `useAnnotationMutationService.ts:226-259`, `classifyPdfSaveRoute.ts:300-309`, and `native/pdf-page-ops/src/annotations.rs:1215-1319`. | Import a sticky note, move it, save, hard-restart, reopen, and compare its page and rectangle with the moved location. | pdf-save / fixed | `eef0c4008`, `1d4034d16` | Red-first classifier, persistence, and native geometry regressions; exact 882-page headless Electron marker drag, native save, hard restart, canonical location check, and qpdf parent/Popup page and rectangle proof passed; focused TypeScript, repository typecheck, touched-file lint, focused architecture, scan cleanup oracle, Rust format, clippy, release tests, and isolated multi-GiB reruns passed. CodeRabbit pass one findings were fixed; pass two was rate-limited. The final validator's three test failures reproduce on current main or pass alone and belong to #144/#147; `fallow:all` reports #147's unused `getAtomicReplaceBackupDestination`. | Complete. |
| PDF-005 | High | product | Browser recent-file pruning can delete durable IndexedDB documents when its localStorage list update fails. | `full-audit.md:29` cites `browserRecentFilesStore.ts:100-125`, `localStorage.ts:20-36`, and `browserDocumentMaintenance.ts:143-166`. | Make the localStorage write fail during pruning; assert the durable IndexedDB records remain and only the intended recent-file list changes. | fixed/published | `63c09bfaf`, test correction `85e2c165e` | Focused Vitest: 5 files, 40 passed; TypeScript 7 tests check passed; staged ESLint passed; CodeRabbit pass 1 returned 0 findings; the full validation attempt also exposed unrelated upstream Electron test failures, and CodeRabbit pass 2 was rate-limited. | Complete. The failed localStorage write no longer authorizes IndexedDB eviction, and failed legacy-cookie migration retains the cookie. |
| PDF-006 | High | product | Native small split, text overlay, and conformance paths can destructively write the requested output, corrupting same-path or hard-linked sources on failure. | `full-audit.md:30` cites `split_pages.rs:673-779`, `text_layer.rs:1593-1632`, and `conformance.rs:48-83`. | Use same-path and hard-link outputs, force a mid-write failure, and assert the source remains byte-identical and readable. | fixed/published | `63c09bfaf` | `cargo fmt --manifest-path native/Cargo.toml --all --check`; workspace clippy with `-D warnings`; release page-ops suite: 184 library tests, 6 error-classification, 7 large-file, 3 provenance, 16 split-page, and 10 text-layer tests passed; CodeRabbit pass 1 returned 0 findings; pass 2 was rate-limited. | Complete. Small split, text-overlay, and conformance writes stage through atomic output and preserve same-path and hard-link inputs in the regression cases. |
| PDF-007 | High | product | Browser page operations can drop both `/Outlines` and `/PageLabels`. | `full-audit.md:31` cites `createBrowserPageOpsCapability.ts:317-517`, `browserPageOpsCore.ts:182-300`, and WASM `page_tree_ops.rs:383-465`; direct before-and-after probe confirmed loss. | Run browser delete, reorder, and insert on a PDF containing both dictionaries; assert both survive save and reopen. | issue-119 / complete | `ab2a65aa6` → `08b854b50` | Browser Chromium acceptance `browserPageOpsAcceptance.test.ts` passed, covering `saves, reopens, and remaps outlines and page labels for delete, reorder, and insert` plus the deleted-outline resurrection regression; focused capability suite 25/25, focused ESLint, TypeScript check, and CodeRabbit final pass 0 findings. Full `node scripts/run-all-gates.mjs --only validate` rerun reached Fallow and typecheck but failed on unrelated shared-host coverage timeouts, then the Electron smoke probe received `ECONNREFUSED 127.0.0.1:3000`; the earlier attempt also saw the documented host hang/failures. | Browser delete, reorder, and insert now save and reopen a PDF with both dictionaries, preserving labels as `1, II`, `II, 1, I`, and `1, 2, I, II`; outline destinations remap to pages 2, 1, and 4. Deleting the outlined page reopens with two page objects and a null outline destination. No PDF-007 acceptance remains. |
| ANN-001 | High | product | Imported native text-markup note edits materialize the whole PDF in PDF.js because native projection omits note text. | `full-audit.md:38` cites `classifyPdfSaveRoute.ts:300-309,382-389` and `nativeMarkupMutations.ts:115-123`. | Import a native text-markup annotation with a note, edit only its note, instrument PDF.js materialization, and assert the native bounded route is used. | annotation-save / fixed/published | `9fcc32c34`, `78e102832`, `4154e6a79` | Native note-text projection and linked Popup `/Contents` postcondition regressions pass. The exact 882-page Electron matrix passed create, geometry and contents update, delete, recreate, one revision per save, hard reopen, and qpdf object checks for Highlight, Squiggly, StrikeOut, and Underline through preload/main. Evidence: `.devkit/analysis/gates/2026-08-30T06-25-06-391Z-3914053-9406d1de.ndjson`. | Complete. |
| ANN-002 | High | product | Imported non-point FreeText edits still use full renderer materialization. | `full-audit.md:39` cites `pdfAnnotationStorageChanges.ts:321-366`. | Import a non-point FreeText box, edit text or style, and assert no full-PDF renderer materialization occurs. | annotation-save / fixed/published | `87aa9e4f1`, `925872307`, `4154e6a79` | Existing-id FreeText native projection and canonical lifecycle regressions pass. The exact 882-page Electron matrix passed native FreeText note and editor creation, geometry, rotation, style and contents updates, delete, recreate, one revision per save, hard reopen, and qpdf object checks through preload/main. Evidence: `.devkit/analysis/gates/2026-08-30T06-25-06-391Z-3914053-9406d1de.ndjson`. | Complete. |
| ANN-003 | High | product | Imported shapes retain stale `/AP` appearance streams after geometry or style edits, so external readers may draw the old appearance. | `full-audit.md:40` cites `applyShapeAnnotations.ts:210-329` and Rust `shapes.rs:317-385`. | Import a shape, change geometry and style, save, reopen in an independent reader or qpdf object probe, and assert the appearance matches the dictionary. | annotation-save / fixed/published | `2dbd84d04`, `4154e6a79` | Semantic subtype-aware `/AP` invalidation and untouched-sibling regressions pass. The exact 882-page Electron matrix passed create, geometry and style update, delete, recreate, one revision per save, hard reopen, native shape-index verification, and qpdf validation for Square, Circle, Line, PolyLine, Polygon, and Ink through preload/main. Evidence: `.devkit/analysis/gates/2026-08-30T06-25-06-391Z-3914053-9406d1de.ndjson`. | Complete. |
| ANN-004 | High | product | Placed images have no canonical reopen, update, or delete lifecycle. They are written as stamps and omitted from the imported entity model. | `full-audit.md:41` cites `applyPlacedImage.ts:15-123` and `nativePdfMutations.ts:655-669`. | Place an image, save, hard-restart, reopen, update it, delete it, and assert each state survives without orphan stamps. | annotation-save / fixed/published | `9fcc32c34`, `14b986201`, `4793119b3` | Canonical managed-Stamp import/reopen identity, browser/native update-delete graph cleanup, and direct native lifecycle tests pass. `4793119b3` refreshes a stale checkpoint expectation only when the original and managed working copy still share the app-published inode; same-inode external edits and distinct-inode replacements remain conflicts. The combined exact 722,178,517-byte fixture passed create, save, hard reopen, update, second save, hard reopen, delete, final save, hard reopen, and qpdf graph/orphan checks in 316 seconds. Evidence: `.devkit/analysis/gates/2026-08-30T07-51-47-613Z-99642-c09a05ff.ndjson`. | Complete. After hard reopen, the save witness refreshes a stale checkpoint only for the app-published linked original and working copy. Both forms of external replacement remain fail-closed. |
| ANN-005 | High | product | Deleting reopened ordinary FreeText from the annotations sidebar removes the canonical rows but leaves the visible text on the page with no remaining sidebar control to remove it. | User recording `CleanShot 2026-08-29 at 11.30.45.mp4` on the admitted 882-page Zaliznyak file: three reopened `Inline Note` rows dated Aug 28 are deleted in sequence, the sidebar count reaches zero, and the green `asdfasdf` plus yellow `adfasdf` page-1 text remain visible. This is not covered by the current saved-highlight replay repairs. | Save and hard-reopen ordinary FreeText, delete it through the sidebar, and assert the canonical row and live PDF.js editor disappear together. Save again, hard-restart, and verify the text and PDF annotation object remain absent through the bounded native route. | annotation-runtime / fixed/published | `b07ed4ab2`, `863f944ca`, `925872307`, `0064bf875`, `48b05406a`, `b9a7bdc64`, `1b36b4f0b`, `df72592c0`, `f992d0654`, `4f89f9a11`, `029e84c4f` | Exact 882-page Electron case `deletes a persisted ordinary FreeText through the sidebar and keeps it absent after restart` passed 1/1 with four title skips. It proved canonical, live-editor, visual annotation, and sidebar counts drop to zero, saved output passes qpdf/object absence checks, and a second hard restart remains empty. The bounded resize-history regression passes 5/5, and the exact Mac issue-139 save/layout-transition case passes 1/1 on the same 722,178,517-byte fixture, preserving painted FreeText, canonical/sidebar counts, and resize-transition visibility through save commit and hard reopen. The follow-up also verifies all five saved `/FreeText` objects after reopen. Adjacent bridge suites 66/66, repository typecheck, targeted ESLint, stylelint, and diff check pass. Evidence: `.devkit/analysis/gates/2026-08-30T01-34-36-890Z-57769-baec30b2.ndjson`, `.devkit/analysis/gates/2026-08-30T11-58-47-248Z-58466-cf325b27.ndjson`, and `.devkit/analysis/gates/2026-08-30T12-38-17-818Z-10739-a3430eb4.ndjson`; exact fixture SHA `1660bced91f628b9acbb2fc0f9dac29fe783a3f43d26231d8f3b0c73133b21b6`. | Complete for this ledger row and the verified Mac resize-transition visibility slice. Broader #139 projection, close-transition, and multi-existing-editor acceptance remains open. |
| ANN-006 | High | product | Restoring a persisted Highlight after a saved delete can geometry-match the retired PDF object after the same incremental mutation replaces it with `Null`, so the save refuses to commit. | Issue #144 headless reproduction logged `object has wrong type; expected type Dictionary but found type Null` and `native-save-required, pre-write` in the saved-delete and undo flow. | At the native append seam, create a managed Highlight, then submit one mutation that deletes its object and recreates the live editor with the same managed name and geometry. Assert the old object remains `Null` and the page points to a fresh annotation object. | fixed/published | `56c561f70`, `f820922d7` | Red-first persisted-revision regression now passes with the owner's in-memory replay regression and the delete-only case; native page-ops 208/208, Rust fmt and clippy, portable WASM freshness, strict Highlight headless target, and focused native projection 13/13 pass. | Complete for this saved-delete/undo row. Issue #144 remains open for its separate drawn-Ink acceptance. |
| ANN-007 | High | product | A redrawn Ink shape can retain the same PDF ref and stable key as a shape tombstone in one native mutation. Native delete retires the object before shape application tries to update it, so the save refuses to commit. | Issue #144 repeated draw-save-delete-redraw reproduction logged the same `Dictionary` versus `Null` refusal and found no `/Ink` annotation on disk. | At the native save projection seam, model a deleted persisted Ink followed by a redraw whose aliases match the tombstone. Assert the live shape becomes a create payload while the old aliases remain in the delete lists. | fixed/published | `2fb34608f`, `0f180fdcf`, `5b260ad9c`; preserved-source reconciliation in this row's completion commit | Red-first projection tests prove retired aliases become a create payload and editor-only `id` stays out of the Rust protocol. Frontier tests hold revision-triggered shape imports behind save preparation, retire them after commit, and exclude canonical managed-shape editor storage from live PDF.js work. The reconciliation test proves managed-shape-only changes clear the preserved PDF.js source flag when its saved fingerprint is unchanged, while real PDF.js editor changes retain it. The unchanged strict repeated-cycle target passes every save and on-disk Ink assertion. | Complete. |
| CAP-001 | High | product | Native mutation arrays reject a whole request above fixed collection caps instead of batching or continuing. Caps are 256 notes or editors, 2,048 page-label ranges, 5,000 bookmarks, 4,096 shapes or markup items, and 16 images. | `full-audit.md:42`; `cap-audit.md:17-41` proves preload, platform, and main normalization enforce the caps. | For each family, invoke a save with `limit + 1` valid entries and assert explicit chunking or sparse continuation rather than pre-native rejection. | native-save / fixed/published | `9fcc32c34`, `de25dea4c`, `2e8ba691d`, `e1857816b`, `4154e6a79` | Contract splitter 14/14, Electron bounded-handler suite 19/19, 10,001-entry page-metadata continuation, native oversized-bookmark append, public aggregate-limit regressions, and the cap-plus-one preload boundary 47/47 pass. The exact 882-page Electron matrix completed every annotation mutation family through preload/main with one logical revision per save and hard-reopen proof. Evidence: `.devkit/analysis/gates/2026-08-30T06-25-06-391Z-3914053-9406d1de.ndjson`. | Complete. |
| SEL-001 | High | product | Selection above 100,000 pages becomes an empty legacy array for agent, exposed API, and menu consumers. | `full-audit.md:46` cites `useWorkspaceViewerShellState.ts:49-53` and `useDocumentWorkspaceAgent.ts:969-1008`. | Select more than 100,000 pages through each consumer and assert the exact compact selection remains available without an empty-array all-pages sentinel. | fixed/published | `bbcaf971c` | Red-first agent, expose, thumbnail, and page-operation regressions; 13-file focused matrix 298 passed; blocking Electron smoke 3/8 passed; TypeScript, ESLint, and diff check passed. | Complete. The verified commit is published on remote main; unrelated baseline failures do not affect this slice. |
| SEL-002 | High | product | Export and print dialogs can convert a partial compact selection into all pages. | `full-audit.md:47` cites `useWorkspaceExport.ts:480-513` and `usePdfPageScopeSelection.ts:45-49`. | Submit a compact partial selection too large for explicit materialization; assert export and print receive exactly that selection, not all pages. | fixed/published | `bbcaf971c` | Red-first export/print regressions; 13-file focused matrix 298 passed; blocking Electron smoke 3/8 passed; TypeScript, ESLint, and diff check passed. | Complete. The verified commit is published on remote main; unrelated baseline failures do not affect this slice. |
| SEL-003 | High | product | Thumbnail context-menu actions apply only to the clicked page instead of the compact multi-selection. | `full-audit.md:48` cites `usePdfThumbnailSelection.ts:231-241` and `pdfPageSelection.ts:62-73`. | Select a compact multi-range, invoke the thumbnail context action on one page, and assert every selected page receives the operation. | fixed/published | `bbcaf971c` | Red-first thumbnail and handler regressions; 13-file focused matrix 298 passed; blocking Electron smoke 3/8 passed; TypeScript, ESLint, and diff check passed. | Complete. The verified commit is published on remote main; unrelated baseline failures do not affect this slice. |
| SEL-004 | High | product | Manual input such as `1-1000000` allocates a million-element array before IPC validation rejects it. | `full-audit.md:49`. | Submit a million-page range under an allocation budget; assert parsing stays compact and rejects or streams before dense allocation. | fixed/published | `bbcaf971c` | Red-first million-page parser and allocation regressions; 13-file focused matrix 298 passed; TypeScript, ESLint, Fallow, duplication, and diff check passed. | Complete. The verified commit is published on remote main; unrelated baseline failures do not affect this slice. |
| SEL-005 | High | process | Extract batches open one PDF, tab, and dialog for every 10,000 pages. | `full-audit.md:50`. | Run extraction over a selection spanning multiple 10,000-page batches and assert bounded session count, cancellation, and cleanup. | fixed/published | `bbcaf971c` | Red-first extraction lifecycle regression; 13-file focused matrix 298 passed; blocking Electron smoke 3/8 passed; TypeScript, ESLint, and diff check passed. | Complete. The verified commit is published on remote main; unrelated baseline failures do not affect this slice. |
| SEL-006 | High | product | Large rotate and delete jobs are not atomic, create one undo item per batch, and are retained behind a 20-entry history limit. | `full-audit.md:51`. | Force failure after an intermediate batch; assert no partial document mutation and one undo item restores the entire user operation. | fixed/published | `bbcaf971c` | Red-first second-batch failure and transaction-journal regressions; 13-file focused matrix 298 passed; blocking Electron smoke 3/8 passed; TypeScript, ESLint, and diff check passed. | Complete. The verified commit is published on remote main; unrelated baseline failures do not affect this slice. |
| SEL-007 | High | product | Crop and remove-crop give only the first 1,024-page batch a revision token, so a later batch can fail after the first mutation commits. | `full-audit.md:52` cites `usePageOperations.ts:296-353` and document mutation guards. | Fail the second crop batch after the first commits; assert rollback or one final committed revision, never a partial success. | fixed/published | `bbcaf971c` | Red-first second-batch crop failure and rollback regressions; 13-file focused matrix 298 passed; blocking Electron smoke 3/8 passed; TypeScript, ESLint, and diff check passed. | Complete. The verified commit is published on remote main; unrelated baseline failures do not affect this slice. |
| CAP-002 | High | product | Page-operation IPC contracts reject explicit arrays or ranges over 100,000 for delete, extract, reorder, move, rotate, crop, remove-crop, and insert-file, while most structural paths have no continuation. | `cap-audit.md:43-60` cites `pageOpsPlatformFeature.ts:39-167`, main bindings, and whole-array callers. | Decode 100,001 valid entries for delete and extract, then run a large rotate and assert bounded multiple native calls with preserved metadata and revision. | fixed/published | `bbcaf971c` | Red-first 100,001-entry codec/rotate regressions; 13-file focused matrix 298 passed; native workspace and sparse-file matrix passed; blocking Electron smoke 3/8 passed; TypeScript, ESLint, and diff check passed. | Complete. The verified commit is published on remote main; unrelated baseline failures do not affect this slice. |

## Export, print, browser fallback, and selected-page limits

| ID | Severity | Class | Finding | Evidence | Required red regression test or probe | Owner/status | Fix SHA | Gates | Remaining acceptance |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| EXP-001 | High | product | Image and TIFF DPI planning parses only the last 262,144 bytes of `pdfinfo` output, so an early huge page can be missed and receive unsafe DPI. | `full-audit.md:56` cites `export.ts:515-590`, `runNativeCommand.ts:50,302-314`, and `appendTextChunkWithByteCap.ts:18-27`; a 6,000-page probe chose 300 DPI instead of safe 58 DPI. | Put a huge-dimension page first in a 6,000-page fixture and assert DPI planning sees it and chooses the safe bound. | fixed/published | `af4ecb65b` | The red-first 6,000-page early-huge-page regression passes at the public export seam; repository typecheck, targeted ESLint, Fallow, and focused architecture checks pass. | Complete. Streamed `pdfinfo` parsing sees the first huge page and applies its safe DPI to every render window. |
| EXP-002 | High | product | Multipage TIFF export retains every rendered page, combines them quadratically, and has no cancellation during descriptor planning. | `full-audit.md:57` cites `export.ts:1082-1195` and the combiner around `export.ts:215-247`. | Run a large multipage TIFF export, cancel during descriptor planning and combination, and assert bounded memory, prompt stop, and no partial success. | fixed/published | `515c55b9f`, `574e9644c` | Red-first descriptor and mid-combine cancellation regressions pass with bounded 25-page render/descriptor/combine windows; image-export 35/35; repository typecheck, targeted ESLint, Fallow, and focused architecture checks pass. | Complete. Each render window combines before the next one, source pages are released promptly, cancellation removes partial work, and final promotion remains atomic. |
| EXP-003 | High | product | Image export returns an unbounded array of output paths through IPC. | `full-audit.md:58`. | Export enough pages to exceed the intended output-path budget; assert a streamed or bounded result protocol rather than one unbounded IPC array. | fixed/published | `5a3598d14`, `416b43968` | Red-first 100,001-path IPC regressions pass, including cleanup. The published export preflight now refuses an over-budget page count before `pdftoppm`; image-export and image-export IPC suites passed 53/53, with repository typecheck, targeted ESLint, Fallow, and focused architecture checks passing. | Complete. The bounded result protocol refuses above 100,000 paths and deletes generated outputs before reporting failure. |
| EXP-004 | High | product | DjVu page-size and preview caches are dense and unbounded; selected export repeatedly scans windows, and a byte-small file over 10,000 pages can enter the browser whole-read cap. | `full-audit.md:59`. | Use a byte-small 10,001-page DjVu, run preview and selected export, and assert sparse/windowed memory and no browser whole-read fallback. | fixed/published | `9fcc32c34` | Focused DjVu/browser/native suite 13/13; combined owned suite 16 files/164 tests; typecheck, ESLint, and diff check green. | Complete. The verified commit is published on remote main. |
| EXP-005 | Medium | product | Path-backed print exposes facing-page and orientation settings that the implementation cannot honor. | `full-audit.md:60`. | Request each exposed facing and orientation mode on a path-backed document; assert the output honors it or the control is unavailable. | fixed/published | `af4ecb65b`, `01e7e6b9c`, `56cc2c22c` | Path-backed PDFs now expose Single, Facing, and orientation controls. Facing maps to Electron's native two-pages-per-sheet option, while portrait and landscape map to the native orientation option. First-page-single stays unavailable because native two-up cannot print the cover as a full sheet. The renderer does not load path-backed PDF bytes. The same change makes the macOS PDF host transparent before showing it and clears preparation feedback through a request-scoped native-dialog event. The focused regression suite passed 147 tests, the affected repository suite passed 1,175 files and 10,305 tests, TypeScript and ESLint passed, and the final CodeRabbit review reported no findings. | Complete for the native path contract. Path-backed printing offers the native layouts it can represent, refuses first-page-single without reading PDF bytes, hides the temporary PDF host, and ends preparation feedback when the system dialog opens. |
| EXP-006 | High | product | A small-byte PDF with a huge page count can enter an eager pdf-lib print path and allocate dense page arrays. | `full-audit.md:61`. | Create a small-byte, high-page-count PDF and run print under a dense-allocation budget; assert the path stays windowed. | fixed/published | `af4ecb65b`, `925872307` | The 5,001-page, three-byte PDF regression stays off pdf-lib and canvas rendering; repository typecheck, targeted ESLint, Fallow, and focused architecture checks pass. | Complete. Page count joins byte size in eager-path admission, and large selections remain compact or refuse before materialization. |
| EXP-007 | High | product | Browser PDF open has a practical 16 MiB ceiling because it performs a whole read and enforces a store cap despite a range validator. | `full-audit.md:62` cites `createBrowserDocumentsFileCapability.ts:510-513` and `browserDocumentRecordStore.ts:243-248`. | Open a browser PDF just over 16 MiB with range reads available; assert it opens through ranges or returns an explicit capacity error without a whole read. | fixed/published | `9fcc32c34` | Browser range/validation suite 42/42; combined owned suite 16 files/164 tests; typecheck, ESLint, and diff check green. | Complete. The verified commit is published on remote main. |
| EXP-008 | High | product | WASM combine bypasses the shared output-size cap. | `full-audit.md:63`. | Combine inputs until the shared output cap is crossed; assert the same cap and typed refusal apply to the WASM route. | fixed/published | `9fcc32c34` | Browser/native combine and worker suite 23/23; Rust pdf-image-combine library 83/83; combined owned suite 16 files/164 tests; typecheck, ESLint, and diff check green. | Complete. The verified commit is published on remote main. |
| EXP-009 | Medium | product | Native DjVu, shape, and desktop operations can silently fall into browser implementations when the bridge is missing. | `full-audit.md:64`. | Remove the bridge and invoke each operation; assert explicit capability routing or refusal, never silent browser substitution. | fixed/published | `9fcc32c34` | Missing-bridge and native-path refusal tests included in the 16-file/164-test owned suite; typecheck, ESLint, and diff check green. | Complete. The verified commit is published on remote main. |
| CAP-003 | High | product | Explicit image-export and DjVu selections over 100,000 pages are rejected, while the workspace can turn a large partial selection into the empty all-pages sentinel. | `cap-audit.md:84-94` cites the image and DjVu contracts, `useWorkspaceExport.ts:480-495`, and the native DjVu windowed path. | Submit a partial selection over 100,000 pages to image export, DjVu export, and print; assert exact selection via ranges or an explicit refusal, never all pages. | fixed/published | `bbcaf971c` | Red-first export/print selection transport regressions; 13-file focused matrix 298 passed; blocking Electron smoke 3/8 passed; TypeScript, ESLint, and diff check passed. | Complete. The verified commit is published on remote main; unrelated baseline failures do not affect this slice. |

## Search and OCR

| ID | Severity | Class | Finding | Evidence | Required red regression test or probe | Owner/status | Fix SHA | Gates | Remaining acceptance |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SRCH-001 | High | product | Native search accepts `PARTIAL` and `TRUNCATED` sidecars as fresh, while agent page search truncates at 30,000 characters without a completeness signal. | `full-audit.md:68` cites `nativeSearch.ts:220-447` and the agent search path. | Feed partial and truncated sidecars plus a long page to native and agent search; assert stale data is rejected and truncation is explicit. | review-fix / complete | `90b414ae1` (pin only, vacuous fixture magic) → `08b854b50` | `cargo test --release` (pdf-page-ops, 185 lib + integration), `cargo fmt --check`, `cargo clippy`, focused Vitest (nativeSearch, managedTempFileHandles, fileOpsPathSecurity, documentsPrint, documentsSavePdfAs, browserPageOpsWasm), `node scripts/run-all-gates.mjs --only validate` green except the pre-existing `pdfjsRangeTransportBounded` hang tracked in #141; CodeRabbit CLI unavailable (included reviews exhausted), fail-open | Review found `90b414ae1` changed no logic and its fixture used magic `EVBSSIDX` instead of `EVBSIDX3`, so the test never reached the flag check. `isNativeSearchIndexFresh` now returns null for `PARTIAL` or `TRUNCATED` coverage and the test proves a complete sidecar reaches native search. Agent page reads already exposed `truncated` and `textLength`. |
| SRCH-002 | High | product | Search normalization can allocate near a gigabyte for one accepted 32 MiB page because streaming limits do not apply to normalized output. | `full-audit.md:69`. | Search a 32 MiB page with normalization expansion under an allocation budget; assert bounded output or typed refusal before excessive allocation. | codex / complete | `ef5aaa406` | `pnpm exec vitest run --project unit-core tests/unit/contracts/search.test.ts` passed | `normalizeSearchText` and match offset normalization refuse output above 8 MiB with `SearchTextBudgetError`, before offset arrays can grow without bound. |
| SRCH-003 | High | product | Browser search lacks query-cost validation, its page-text cache ignores the advertised byte budget, and a persisted unsafe page count can drive an enormous loop. | `full-audit.md:70`. | Persist an unsafe page count and issue a high-cost query; assert validation, bounded cache growth, and bounded iteration. | codex / complete | `1e2154513` | `pnpm exec vitest run --project unit-app tests/unit/app/platform/browserSearchLimits.test.ts tests/unit/app/platform/browserSearchWorker.test.ts tests/unit/app/platform/browserSearchCore.test.ts tests/unit/app/platform/searchCapability.test.ts` passed; focused ESLint command started but exceeded the 30s command window without diagnostics; full validate reached type coverage but emitted no terminal status | Browser validates query cost and page counts before extraction loops; persisted page counts are capped; page-text cache evicts over-budget bytes. |
| SRCH-004 | High | product | OCR availability treats corrupt artifacts as available, and v3 migration can publish malformed artifacts. | `full-audit.md:71`. | Corrupt an OCR artifact and run availability and migration checks; assert corruption is unavailable and malformed output is never published. | fixed/published | `9fcc32c34` | Full local coverage 1,158 files/9,992 tests; OCR catalog focused tests green; typecheck, ESLint, and diff check green. | Complete. The verified commit is published on remote main. |
| SRCH-005 | High | product | OCR text windows can return a stale revision because snapshot and 64-page budgets are checked after materialization. | `full-audit.md:72`. | Change the source during OCR window materialization; assert stale revision is rejected before returning text and budgets are enforced before allocation. | fixed/published | `9fcc32c34` | Full local coverage 1,158 files/9,992 tests; OCR window/revision focused tests green; typecheck, ESLint, and diff check green. | Complete. The verified commit is published on remote main. |
| SRCH-006 | High | product | OCR cancellation is ignored in several paths, including DOCX generation, and the degraded raster guard runs after expensive rendering. | `full-audit.md:73`. | Cancel OCR and DOCX generation before and during raster work; assert no later render or output commit occurs and guard runs before allocation. | fixed/published | `9fcc32c34`; `5c05e207e`, `9ef3f01b0`, `961185b18` | Full local coverage 1,158 files/9,992 tests with zero-execution tripwire green; the deprecated `useOcr` DOCX path now passes an AbortSignal through async generation and returns silently on AbortError; DOCX/component focused tests 6/6 plus the new cancellation regression; managed temp-file suite 25/25; typecheck, ESLint, and diff check green. | Complete. The verified cancellation commits are published on remote `main`; any remaining OCR catalog acceptance stays in its separate rows. |
| SRCH-007 | Medium | product | OCR page-identity sidecars accept trailing garbage. | `full-audit.md:74`. | Append bytes after a valid page-identity sidecar and assert strict parsing rejects the artifact. | fixed/published | `9fcc32c34` | Full local coverage 1,158 files/9,992 tests; page-identity sidecar focused tests green; typecheck, ESLint, and diff check green. | Complete. The verified commit is published on remote main. |

## Scan cleanup and rendering

| ID | Severity | Class | Finding | Evidence | Required red regression test or probe | Owner/status | Fix SHA | Gates | Remaining acceptance |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SCAN-001 | High | product | Auto cleanup above 1,024 pages disables compact-source preservation and its budget. | `full-audit.md:78`. | Run auto cleanup on a document just below and above 1,024 pages; assert compact-source preservation and budget behavior remain explicit and bounded. | implemented / acceptance open | `9fcc32c34`, `58498c412` | Published source/test fix; full-run lossless budgeting now counts compact pages across bounded batches and fails closed when probing is incomplete; focused lossless/workspace suites pass (104/104), affected scan-cleanup oracle passes with native catastrophes 0, stroke-weight and word-loss checks green; exact xlarge acceptance remains open. | Preserve compact source data or fail with a documented capacity result above the threshold. |
| SCAN-002 | High | product | Sparse lossless selection races a shared mutable page-size cursor. | `full-audit.md:79`. | Run concurrent sparse lossless selections with different page-size windows; assert each result uses its own immutable cursor and dimensions. | implemented / acceptance open | `9fcc32c34`, `14cb022b9` | Published source/test fix; focused DjVu preview/page-size coverage and affected scan-cleanup oracle pass; concurrent different-window isolation on the exact xlarge path remains open. | Remove shared mutable cursor state and prove concurrent selection isolation. |
| SCAN-003 | High | product | More than 20,000 pages evicts ink-placement anchors and falls back to top-center placement. | `full-audit.md:80`. | Place ink on pages beyond 20,000, save, reopen, and assert coordinates remain anchored to the intended page region. | implemented / acceptance open | `0e8557ccc`, `0320f83e8`, `58498c412` | Bounded document-wide calibration summary and per-batch anchor projection; xlarge requests omit duplicate retained-page anchor maps when a leased summary is present; workspace/pipeline/service suites pass (194/194), affected oracle pass with placement identity true, no violations, and zero word-loss flags; Electron typecheck, targeted ESLint, and diff check passed. | The conversion path no longer builds a document-sized anchor map. Exact >20,000-page save, hard-restart, and coordinate proof remains open. |
| SCAN-004 | Medium | process | The xlarge representation report can be empty and point to deleted scratch JSONL. | `full-audit.md:81`. | Delete or truncate scratch JSONL during representation reporting; assert the report fails closed with a retained diagnostic, not an empty success. | implemented / acceptance open | `9fcc32c34` | Published source/test fix; focused detection and pipeline coverage passes; affected scan-cleanup oracle passes; exact xlarge artifact ownership and retention proof remains open. | Make report artifact ownership and retention explicit through success and failure. |
| SCAN-005 | High | product | Renderer metadata remains dense through 20,000 pages, and the source-DPI Promise cache is unbounded. | `full-audit.md:82`. | Open a 20,000-page document and inspect metadata and source-DPI cache growth under a memory budget; assert bounded or sparse structures. | implemented / acceptance open | `9fcc32c34` | Published source/test fix; focused preview/cache coverage passes; affected scan-cleanup oracle passes; exact xlarge residency measurement remains open. | Bound metadata and cache lifetime by windows, revisions, or explicit capacity policy. |
| SCAN-006 | High | product | Scan-cleanup IPC coerces unsafe numbers and preserves rotation as a string. | `full-audit.md:83`. | Send unsafe numeric coordinates and string rotation through IPC; assert strict rejection or typed normalized values. | fixed/published | `9fcc32c34` | Published contract/native protocol fix; focused scan-cleanup IPC coverage passes; affected scan-cleanup oracle passes; typecheck, ESLint, and diff check green. | Complete. The verified commit is published on remote `main`. |
| SCAN-007 | Medium | process | A stale transient background page has no guaranteed cleanup, and annotation-layer abort controllers survive teardown. | `full-audit.md:84`. | Create and tear down transient pages during active annotation work; assert page disposal and abort-controller cancellation before the session settles. | implemented / acceptance open | `9fcc32c34` | Published annotation-sync/lifecycle fix; focused teardown coverage passes; affected scan-cleanup oracle passes; exact transient-page close/restart proof remains open. | Add teardown ownership and prove no stale page or controller survives close and restart. |
| SCAN-008 | High | product | The thumbnail spacer can exceed Chromium's scroll extent at about 138,000 pages. | `full-audit.md:85`. | Open a document near 138,000 pages, scroll to the end, and assert usable geometry without overflow or unreachable pages. | viewer / implemented; acceptance open | `9fcc32c34`, `875642009` | Segmented thumbnail geometry, dynamic-height mapping, cross-segment keyboard focus, and scroll restoration regressions pass; sparse page-source layout projection stays lazy; unit-app focused viewer gate 10 files/90 tests, targeted ESLint, vue-tsc, and diff check pass. | Live 138,000-page Electron reachability and memory evidence remains open. |
| SCAN-009 | High | product | Outlines truncate silently above 10,000 entries. | `full-audit.md:86`. | Import or create more than 10,000 outline entries, save, reopen, and assert all entries remain or an explicit limit refusal appears. | viewer / implemented; acceptance open | `9fcc32c34`, `93e51b67f` | Iterative 10,001-entry outline parse/resolution remains editable; native bookmark depth boundary is explicit; focused outline/helper gate 32/32, targeted ESLint, and diff check pass. | Native 10,001-entry save/reopen proof still belongs to the continuation lane and remains open. |

## Security, native boundaries, and resource ownership

| ID | Severity | Class | Finding | Evidence | Required red regression test or probe | Owner/status | Fix SHA | Gates | Remaining acceptance |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| SEC-001 | High | product | App-temp read authorization checks only that a path is under the shared temp root, allowing another renderer to read, stat, range-read, or probe a learned path. | `full-audit.md:90` cites `documentFilePathResolution.ts:114-129,258-260,607-625` and `pathValidator.ts:293-295`. | Give renderer B renderer A's temp path and attempt each read, stat, range, and probe operation; assert authorization fails. | review-fix / complete | `4496c7e3d` → `08b854b50` | `cargo test --release` (pdf-page-ops, 185 lib + integration), `cargo fmt --check`, `cargo clippy`, focused Vitest (nativeSearch, managedTempFileHandles, fileOpsPathSecurity, documentsPrint, documentsSavePdfAs, browserPageOpsWasm), `node scripts/run-all-gates.mjs --only validate` green except the pre-existing `pdfjsRangeTransportBounded` hang tracked in #141; CodeRabbit CLI unavailable (included reviews exhausted), fail-open | Review found lease paths were never removed from `leaseIdsByCanonicalPath`, so any leased path (including user-picked images) stayed denied after expiry or revoke; `4496c7e3d` also left `fileOpsPathSecurity`, `documentsPrint`, and `documentsSavePdfAs` red because their `pathValidator` mocks lacked the new exports. Leases now unregister their path on every removal, rebind re-registers the new path, and expired paths fall back to the ordinary read checks. |
| SEC-002 | Medium | product | Raw-path aliases such as `/var` and `/private/var` can defeat working-copy cancellation and range-cache identity while cleanup runs. | `full-audit.md:91`. | Start an operation through one alias and cancel or clean through the other; assert one canonical identity controls both. | issue-123 / fixed/published | `89d80a06a`, `9d016f200` | Red-first Mac regressions proved raw aliases missed lifecycle cancellation and range-handle invalidation. `9d016f200` canonicalizes lifecycle comparisons and every range-cache handle, epoch, pending-read, and byte-budget key. Focused coverage passed 6 files/154 tests; Electron and tests TypeScript, ESLint, diff checks, and CodeRabbit pass 1 passed. Pass 2 was rate-limited without paid use. | Complete. `/var` and `/private/var` now share cancellation, cache, pending-read, and per-document budget identity on macOS. |
| SEC-003 | High | product | `ensureWorkingCopyDirectory` can duplicate work when it races a lazy working copy's background materializer. | `full-audit.md:92`. | Race explicit directory ensure against background materialization; assert one copy, one publication, an unchanged revision token, and no duplicate publication. Materialization is deliberately not a revision-advancing save. | issue-123 / fixed/published | `89d80a06a` | Focused concurrent materialization and creation suites passed with one operation ID; the direct real-filesystem race probe passed 2/2, with the two explicit ensures joining the background flight, matching bytes, and the document revision unchanged; ESLint, TypeScript, and diff checks passed. | Complete. Concurrent background and explicit materialization share one operation and one unchanged revision token. |
| SEC-004 | High | product | Finite JavaScript `f64` values can overflow Rust `f32` fields to infinity or NaN and produce invalid PDF coordinates. | `full-audit.md:95`. | Send finite values near and beyond `f32` range through the mutation contract; assert typed rejection or finite clamping before native code. | fixed/published | `63c09bfaf` | Focused native-mutation contract tests: 15 passed; TypeScript 7 tests check passed; native page-geometry conversion assertion covered by the release page-ops suite; workspace clippy and formatting passed; CodeRabbit pass 1 returned 0 findings; pass 2 was rate-limited. | Complete. The IPC contract rejects finite values outside native `f32` range, and native PDF real-number conversion rejects non-finite results. |
| SEC-005 | High | product | Bookmark validation recurses into nested children before enforcing depth, so a deeply nested request can overflow the stack. | `full-audit.md:96`. | Submit a nesting depth beyond 64 with enough nodes to stress recursion; assert bounded rejection without stack overflow. | fixed/published | `63c09bfaf` | Deep bookmark regression passed in the native release page-ops suite; focused native-mutation contract tests: 15 passed; TypeScript 7 tests check passed; workspace clippy and formatting passed; CodeRabbit pass 1 returned 0 findings; pass 2 was rate-limited. | Complete. Deep bookmark JSON is rejected as a typed invalid request before native output without a stack overflow. |
| SEC-006 | High | product | Annotation and page-size sidecars have no aggregate cap, and ink annotations allow unbounded inner stroke arrays. | `full-audit.md:97`. | Submit many individually valid sidecars and a large stroke array under an aggregate memory budget; assert bounded refusal or streaming. | fixed/published | `61be439a2`, `2e8ba691d` | Contract aggregate budget 19/19, Electron bounded-handler 19/19, native page-ops library 206/206, public nested-shape `save-mutations` refusal 1/1, cargo fmt, clean-tree typecheck, targeted ESLint, and diff check passed. | Complete. The verified fix is published on remote main and issue #138 is closed. |

## Test, CI, and acceptance process

| ID | Severity | Class | Finding | Evidence | Required red regression test or probe | Owner/status | Fix SHA | Gates | Remaining acceptance |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| TEST-001 | High | test/process | Required PR and push CI never runs the exact 722,176,299-byte or 2,168,527,413-byte fixture; required lanes use sparse synthetic 431-page files. | `full-audit.md:108`; `linux-flow-audit.md:16,74-82`. | Run a normal PR or main-push topology check and assert the required lane admits the exact fixture identities or reports an explicit opt-in boundary. | ci-policy / fixed, published | `9fcc32c34`, `6f48ad3d2` | issue136CiPolicy and validationGatePolicy unit policy 39/39; scripts TypeScript; ESLint; fallow | Decision encoded: the required exact-fixture lane admits only the manifest identities (882-page `1660bced…`, 722,178,517 bytes; 2,646-page `5609c151…`, 2,168,527,413 bytes) when the runner names `EVB_EXACT_FIXTURE_PROFILE`; an unset profile fails with `EXACT_FIXTURE_OPT_IN_REQUIRED` instead of a local default, so fixture absence cannot produce a green lane. Hosted CI names the profile in every lane (`ci.yml`). |
| TEST-002 | High | test/process | Native source changes can run Rust work without Electron save coverage, and direct source pushes can match no hosted workflow paths. | `full-audit.md:109`; `linux-flow-audit.md:199-209` confirms classifier and workflow separation. | Change a native save dependency, run the changed-area classifier, and assert an Electron save path is required before green publication. | ci-policy / implemented; acceptance open | `9fcc32c34` | Native-save dependency classification now selects Electron save/reopen, pressure smoke, and native integration; changed-area topology 26/26 and exact-fixture lane passed in run 33276462757. | Keep the current dependency classifier and document any intentionally advisory or skipped lane. |
| TEST-003 | High | test/process | The required large acceptance helper can synthesize annotation state, inject contenteditable values, and call exposed handlers without a real PDF.js annotationStorage mutation. | `full-audit.md:110`; `linux-flow-audit.md:149,161-167`. | Make the acceptance test fail when annotationStorage is not dirty and require visible controls plus real pointer or keyboard input. | fixed/published | `0233b83b3`, `a175f53cd`, `63211b435`; verified at `89432c542` | The exact quiet-host 882-page and 2646-page lanes used visible controls, real pointer/keyboard input, live PDF.js dirty-state checks, and strict save barriers. Focused acceptance gates and both exact wrappers passed. | Complete for issue #134. |
| TEST-004 | High | test/process | `saveViaWindowHandle()` hides missing or rejected `save-committed` events behind DOM-idle fallback and reopens without committed revision or hash proof. | `full-audit.md:111`; `linux-flow-audit.md:175-179` cites `viewerCore.ts:1335-1381`. | Suppress or reject the event probe while DOM becomes idle; assert the save fails rather than passing on fallback. | fixed/published | `0233b83b3`, `a175f53cd`, `9d9ffb44c`; verified at `89432c542` | The exact quiet-host lanes required each visible Save to produce the expected committed revision and path event before hard restart. Both exact wrappers passed. | Complete for issue #134. |
| TEST-005 | Medium | test/process | Blocking PR smoke skips all pressure large-PDF tests; pressure tests open generated files but do not save annotations. | `full-audit.md:112`. | Inspect blocking project membership and run a pressure save; assert at least one bounded annotation-save case executes in the required lane. | fixed/published | `9fcc32c34` | Current-head Electron blocking smoke passed with a bounded annotation save and fresh-process reopen in run 33276462757; policy/topology focused tests 26/26 pass. | Complete. The blocking lane now exercises the pressure save/reopen path. |
| TEST-006 | Medium | test/process | Headless launch settings can disable focus and permit injected state, so required Linux work may not prove that visible product controls received keyboard or pointer input. | `full-audit.md:113`; `linux-flow-audit.md:191-197`. | Run the hidden Electron acceptance flow against visible product controls, dispatch real pointer and keyboard events, and assert focus, event delivery, live annotation storage, and dirty state without JavaScript state injection. | fixed/published | `0233b83b3`, `a175f53cd`; verified at `89432c542` | The exact quiet-host 882-page and xlarge lanes used visible controls, real pointer and keyboard events, live PDF.js storage, and strict dirty-state assertions. | Complete for issue #134. |
| TEST-007 | High | test/process | Teardown swallows heartbeat, RSS, and session-stop failures; timeout races leave CDP work pending, kill errors ignored, and no final child-liveness assertion runs. | `full-audit.md:114`; `linux-flow-audit.md:181-189`. | Inject each teardown failure and a timed-out CDP task; assert the test fails, cancels work, and proves no child remains. | fixed/published | `d1faac4b7`, `208723142` | Focused `unit-electron` teardown/deadline suite 23/23, stop-residue probe 1/1, graceful-shutdown policy 15/15; combined lint/typecheck passed, unit 9680/9680, fallow pre-L0 pair only. | Complete. `runElectronE2ETeardown` aggregates every failed step, deadline polling observes abort, and `stopSingleSession` retains artifacts and fails when a session-owned Electron process survives. |
| TEST-008 | Medium | test/process | Rendered-page assertions check only CSS class, canvas dimensions, and minimal viewport intersection, so blank or stale canvases can pass. | `full-audit.md:115`. | Present a blank or stale canvas with matching dimensions and assert the rendered-page probe fails. | fixed/published | `0233b83b3`; verified at `230ba5205` | The quiet-host 2026-08-30 exact xlarge lane passed 2/2. Its current-page identity, viewport, and sampled nonblank-pixel probes passed for pages 1, 1323, and 2646 in 1616.1 ms, 3799.6 ms, and 3410.7 ms. Telemetry: `.devkit/test/electron-e2e-artifacts/xlarge-document-acceptance.json`; wrapper evidence: `.devkit/analysis/gates/2026-08-30T00-42-44-909Z-3212604-4553f90b.ndjson`. | Complete. The stale or blank canvas regression and the exact 2646-page render probes pass. |
| TEST-009 | Medium | test/process | Unit save tests mock native commands, staged artifacts, working-copy store, atomic replace, and file copy; no tiny-PDF integration test exercises the native binary, qpdf, and `copyFileAtomic`. | `full-audit.md:116`. | Run a tiny real PDF through the native binary, qpdf, and `copyFileAtomic`, including failure and reopen paths. | fixed/published | `9fcc32c34` | Native PDF integration and Electron save/reopen lanes passed in current-head run 33276462757, with qpdf and failure-preservation coverage. | Complete. The native binary, qpdf, atomic copy, failure, and reopen boundaries are covered. |
| TEST-010 | High | test/process | Quarantine uses `--passWithNoTests`, `continue-on-error`, and environment-gated skips, allowing green results with zero cases or known failures. | `full-audit.md:117`. | Run quarantine with zero discovered tests and a known failing case; assert the job fails closed. | ci-policy / fixed, published | `9fcc32c34`, `89c05f7e7`, `6f48ad3d2` | quarantineGraduationPolicy and issue136CiPolicy unit policy 39/39; scripts TypeScript; ESLint; fallow | Decision encoded: every `graduation-policy.json` entry names its GitHub issue, an `expiresOn` date and the concrete assertion-report suite it is judged by; `assertQuarantinePolicy` fails the quarantine job on a past expiry, an unreferenced report suite, a duplicate, or a declared entry with no concrete report. Current entries expire 2026-09-30. |
| TEST-011 | High | test/process | The xlarge renderer IPC probe is optional, and semantic assertions load the whole file while checking only limited object fields, missing page identity, appearance blankness, marker rectangle, Popup parent, and visible result. | `full-audit.md:118`; `linux-flow-audit.md:169-173`. | Disable the optional probe and corrupt each omitted field; assert the acceptance test fails with independent structural and visible checks. | fixed/published | `f5fd24869`, `5ef250df6`; verified at `915cc658d` and `0a8c63289` | Final hidden macOS and sandboxed hidden Linux runs required the runtime IPC probe and independently checked page, Contents, NM, AP, Rect, Popup parent, restored visible note text, and revision after both saves. | Complete. The strict exact-fixture lane fails closed on a missing probe or structural mismatch. |
| TEST-012 | High | test/process | The annotation-save SLO is 8 seconds while exact-fixture evidence records 30 to 47 seconds; other helpers allow 360-second work without cancellation. | `full-audit.md:119`. | Run the exact fixture with the stated SLO and cancellation deadline; assert timeout cancels all work and reports the actual phase. | fixed/published | `0233b83b3`, `30c8a3b6c`, `85670d391`; verified at `230ba5205` | Quiet-host 2026-08-30 exact lanes passed their own save policies. The 882-page lane passed 3/3 and all six recorded native renderer save totals were below its 8000 ms SLO: 2451.7, 2946.0, 3034.7, 3171.3, 3382.9, and 4153.4 ms. The xlarge visible save wait took 12048.1 ms, saved-output `qpdf --check` took 7990.9 ms, and fresh-renderer reload took 20294.2 ms, all within the xlarge 120000 ms cancellation deadline. Xlarge telemetry: `.devkit/test/electron-e2e-artifacts/xlarge-document-acceptance.json`; xlarge wrapper: `.devkit/analysis/gates/2026-08-30T00-42-44-909Z-3212604-4553f90b.ndjson`; 882 wrapper: `.devkit/analysis/gates/2026-08-30T01-04-14-976Z-3250624-e220a53e.ndjson`; 882 save telemetry logs: `.devkit/scratch/dev-server-logs/e2e-run-mtf3wrfr-3fa109-large-pdf-1788051879592/`. | Complete. Save deadlines are phase-aware and cancel the owning session on timeout. Both exact fixtures passed. |
| TEST-013 | Medium | test/process | The save benchmark leaks process environment into another test, `vi.clearAllMocks()` leaves implementations installed, and generic console or unhandled errors do not fail the unit suite. | `full-audit.md:120`. | Run the benchmark before and after a test with different environment and mocks, plus injected console and unhandled errors; assert isolation and failure. | fixed/published | `18e77d304`, `b0d39c76a`, `bf0429fd3` | Edited unit suites 322/322, commit-attribution policy 41/41, console-error probe 1/1, fixture-source policy 10/10; combined lint/typecheck passed, unit 9680/9680, fallow pre-L0 pair only. | Complete. E2E specs pass owned `extraEnv` to their session, unit setup restores its console spy after each test, and unexpected non-Vue `console.error` fails the test. Config-wide `mockReset` remains out of scope because three existing suites intentionally construct module-scope mocks. |
| TEST-014 | High | test/process | Xlarge fixture admission checks regular-file status, byte size, staged size, and qpdf page count but no SHA-256 or content identity. | `cap-audit.md:84-101`; `linux-flow-audit.md:23,86-99`; `full-audit.md:127`. | Replace the configured source with a same-size, same-page-count different PDF; assert admission rejects it by hash or immutable content identity. | fixed/published | `f5fd24869`; verified at `915cc658d` and `0a8c63289` | The final hidden macOS and sandboxed hidden Linux modes admitted exactly 722178517 bytes, 882 qpdf pages, and SHA-256 `1660bced91f628b9acbb2fc0f9dac29fe783a3f43d26231d8f3b0c73133b21b6` before interaction. | Complete for the exact Zaliznyak lane. |
| TEST-015 | High | test/process | No saved-output `qpdf --check` is required by the xlarge flow. | `full-audit.md:127`; `linux-flow-audit.md:169-173,220`. | Corrupt a committed output after save and before reopen; assert the acceptance test runs `qpdf --check` and fails. | fixed/published | `f5fd24869`; verified at `915cc658d` and `0a8c63289` | The strict test runs `qpdf --check` after each committed save and repeats bounded object validation after both hard restarts. Both final hidden platform modes passed. | Complete for the exact Zaliznyak lane. |
| TEST-016 | Medium | test/process | The persistent `startd` path has no shell EXIT cleanup trap, so interruption can leave Xvfb and session children for the stale-session pruner. | `full-audit.md:114`; `linux-flow-audit.md:195,221`. | Interrupt a `startd` run and assert Xvfb, Electron, and session children are cleaned or retained with an explicit failure. | fixed/published | `2fb738643`, `39513ae65` | Linux-branch interruption regression passed 4/4 with no surviving fake Xvfb, session process, pid file, or temp root; combined lint/typecheck passed, unit 9680/9680, fallow pre-L0 pair only. | Complete. `startd` and `restartd` trap INT, TERM, and HUP, stop the owned session, remove an Xvfb started by that invocation, and report a refused stop. |
| TEST-017 | Medium | test/process | The verified xlarge run exceeded the renderer heartbeat limit, reaching 3,350 ms against a 3,000 ms policy. | `full-audit.md:127`; `linux-flow-audit.md:27,55,132-143,223`; telemetry scalar summary records `maxGapMs: 3350`. | Repeat the exact xlarge run and assert the measured heartbeat, save phase, and policy decision are recorded without hiding the failure. | fixed/published | `0233b83b3`, `c5efe2fa1`, `85670d391`; verified at `230ba5205` | The quiet-host 2026-08-30 exact xlarge lane passed 2/2 under a pre-run one-minute load of 2.87. SetInterval, MessageChannel, and Worker max gaps were 646.6/772.8/153.9 ms for Session A open/render/close; 8599.8/9105.3/254.1 ms for Session B pre-save placement; 726.2/902.1/248.7 ms for `save-before-fresh-renderer-reopen`; and 939.3/999.9/205.6 ms for fresh-renderer reopen/final-index. The 5000 ms policy applies only to the actual save window, which passed at 726.2 ms. The pre-save placement gap remains recorded for #149 and does not change this row's result. Telemetry: `.devkit/test/electron-e2e-artifacts/xlarge-document-acceptance.json`; wrapper evidence: `.devkit/analysis/gates/2026-08-30T00-42-44-909Z-3212604-4553f90b.ndjson`. | Complete. The exact xlarge rerun records the stage boundary and passes the save-window heartbeat policy without hiding the separate pre-save gap. |
| TEST-018 | Medium | test/process | Linux xlarge staging with `COPYFILE_FICLONE_FORCE` returned `ENOTSUP`; the run needed a temporary preload that stripped the flag. | `full-audit.md:128`; `linux-flow-audit.md:54,101,122-132`. | Run exact-fixture staging on Linux filesystems with and without clone support; assert a supported clone or tested streaming fallback is selected without source edits. | ci-policy / fixed, verified on Linux | `9fcc32c34` | stageExactPdfFixture run on 2026-08-30 without source edits | Verified both filesystems with the unchanged staging script on the exact 882-page fixture: on a reflink-enabled XFS loop mount (`/mnt/evb-xfs`, `reflink=1`) `--mode=clone` staged with `copyMode: clone` and the staged file shares its single extent with the source (`fiemap` flag `0x2001`); on ext4 `--mode=auto` selected `copyMode: stream` and `--mode=clone` failed (exit 1, `Exact PDF fixture clone staging failed`) rather than reporting a green clone. No preload or flag stripping was needed. |
| TEST-019 | High | test/process | The verified 882-page Linux annotation flow left `annotationDirty: true`, returned `saveSucceeded: false`, and omitted the new FreeText, but helper and cleanup failures prevent a clean product diagnosis. | `full-audit.md:126`; `linux-flow-audit.md:115-120,222,227`. | Reproduce with strict interaction, live storage proof, committed-event observation, and independent output checks; classify product failure only after harness errors are absent. | fixed/published | `f5fd24869`, `5ef250df6`; verified at `915cc658d`; re-verified at `0a8c63289` | Sandboxed hidden Linux session `e2e-run-mtcpy01g-f3be4e-large-pdf-1787907488727` passed real sticky-note pointer and keyboard interaction, live storage and dirty proof, immediate same-renderer continuity, exact commits, bounded object checks, both hard restarts, runtime IPC, and strict teardown. | Complete. Final current-commit Linux evidence is headless per user instruction. |
| TEST-020 | Medium | test/process | The native split-pane test hung for more than six minutes after opening a one-page document; no child residue remained after interruption. | `linux-flow-audit.md:52-53,115-120`. | Run the isolated split-pane test with a bounded timeout and process-tree probe; assert it completes or fails with a typed timeout and no residue. | fixed/published | `5edfd758d` | The current direct-handoff gate uses the two-page large-PDF fixture, waits for PDF.js loaded and interactive state after each native preview open, and checks four independent pane working copies plus page state. | Current main keeps `openNativePdfPreviewInApp` as the active consumer. The bounded K replacement was omitted because the current direct-handoff contract supersedes that four-pane flow. |
| TEST-021 | High | test/process | The xlarge flow does not require or observe the exact `save-committed` automation event. | `linux-flow-audit.md:20,157-165`; `full-audit.md:127,130`. | Remove the event and let other save barriers complete; assert the acceptance test fails instead of passing. | fixed/published | `f5fd24869`; verified at `915cc658d` and `0a8c63289` | The strict test captures the target-specific event before each save action, checks its path and revision, and fails on timeout or mismatch. Both final hidden platform runs passed. | Complete for the exact Zaliznyak lane. |
| TEST-022 | High | test/process | The xlarge flow uses renderer `page.reload()` after save rather than a hard Electron process restart. | `linux-flow-audit.md:21,159-165`; `full-audit.md:127,130`. | Stop Session B after save, start a fresh Session C, reopen the committed path, and assert annotations and revision survive process death. | fixed/published | `f5fd24869`; verified at `915cc658d` and `0a8c63289` | Both final hidden platform runs killed Electron after the first save, required a different process identity, restored through the product checkpoint path, and repeated the process after the second save. | Complete for the exact Zaliznyak lane. |
| TEST-023 | High | test/process | The xlarge flow performs one save only and has no second save after reopen. | `linux-flow-audit.md:22,165`; `full-audit.md:127,130`. | After hard restart and reopen, make a second annotation edit, save, stop, reopen again, and assert both commits. | fixed/published | `f5fd24869`; verified at `915cc658d` and `0a8c63289` | Both final hidden platform runs edited the restored sticky note, created a second note, saved natively, hard-restarted again, and verified both commits. | Complete for the exact Zaliznyak lane. |
| TEST-024 | Medium | product | Native projection correctness on the exact Zaliznyak file remains unproven. | `linux-flow-audit.md:229-236`, especially line 231; `full-audit.md:127,130`. | Run the exact hash-admitted file through native annotation projection and independent object and visible-result checks. | fixed/published | `5ef250df6`; verified at `915cc658d` and `0a8c63289` | Final hidden Mac and sandboxed hidden Linux runs independently verified Contents, stable NM, AP, Rect, Popup parent, qpdf structure, restored text, revision, and runtime IPC after each native commit. | Complete for this sticky-note projection. Other annotation defects remain separate ledger rows. |
| TEST-025 | High | product | Save durability across actual Electron process death and restart remains unproven. | `linux-flow-audit.md:232`; `full-audit.md:130`; xlarge telemetry only records renderer reload at `xlarge-followup-telemetry.json:72-82`. | Save, kill the Electron process, start a fresh process, reopen the committed file, and assert the persisted annotation and revision. | fixed/published | `f5fd24869`; verified at `915cc658d` and `0a8c63289` | Final hidden Mac and sandboxed hidden Linux runs killed and restarted Electron twice, matched revision tokens, checked hashes and qpdf structure, and restored both notes cleanly. | Complete for the exact Zaliznyak lane. |
| TEST-026 | Medium | process | Absence of leaked PDF.js or native work after forced or timed-out save remains unproven. | `linux-flow-audit.md:236`; `full-audit.md:114,130`. | Force save timeout and process stop, then inspect child processes, leases, pending tasks, and artifact handles before declaring completion. | fixed/published | `ec5f3a2c9` | Isolated save-pipeline case passed 1/1 in 25.6 s. While the staged native commit barrier was held, hard stop left no captured child, session-owned Electron, `lsof` handle, or partial source mutation; hash stayed unchanged and independent reopen contained no FreeText. Combined lint/typecheck passed, unit 9680/9680, fallow pre-L0 pair only. | Complete. Renderer-side stalled-save timeout remains pinned by `workspaceSaveServiceNativePersistence.test.ts`; this process-death probe covers the native staged-save residue boundary. |
| TEST-027 | High | test/process | The xlarge flow does not compare a pre-save and post-save structural object table, so its qpdf checks are incomplete. | `linux-flow-audit.md:171-173`; `full-audit.md:127`. | Capture bounded pre-save and post-save object identity and appearance summaries, then assert only intended objects changed. | fixed/published | `0233b83b3`; verified at `230ba5205` | The quiet-host 2026-08-30 exact xlarge lane passed its saved-output `qpdf --check` in 7990.9 ms and its bounded structural comparison. The baseline had six entries, the final table had ten, all six baseline object references were unchanged, and exactly four intended annotation objects were added. Telemetry recorded `failure: null` at `.devkit/test/electron-e2e-artifacts/xlarge-document-acceptance.json`; wrapper evidence: `.devkit/analysis/gates/2026-08-30T00-42-44-909Z-3212604-4553f90b.ndjson`. | Complete. The exact fixture passed independent qpdf, bounded object-identity, appearance, and semantic reopen checks. |
| TEST-028 | High | test/process | On macOS the session stopper treated an unreaped zombie controller as alive: `kill(pid, 0)` succeeds for zombies, so the identity probe saw `<defunct>`, refused the stop, retained artifacts, and the non-clean hard restart in `largePdfAnnotationSave.e2e.test.ts` failed under load. | Lane W acceptance logs `evb-viewer-lanes/w/.devkit/tmp/acceptance-1.log` and `acceptance-2.log` (exact 722 MB fixture; both fail at `Stopping Electron E2E session ... for restart` with `stop was refused ... session artifacts were retained`). | Spawn an unreaped zombie and assert `isProcessAlive` reports it exited and `stopSingleSession` removes the session artifacts without a refusal warning (`tests/unit/scripts/electronRunGracefulShutdownPolicy.test.ts`). | fixed/published | `eeef900c3`, `fe7c8e447` | lint, typecheck, unit (9669 passed), fallow (pre-L0 pair only); acceptance: sticky-note hard restart on the exact fixture 2/2 (`evb-viewer-lanes/k/.devkit/tmp/acceptance-1.log` 132 s, `acceptance-2.log` 123 s) | Complete. The stopper treats an unreaped zombie as exited, removes the session artifacts, and preserves the exact-fixture hard-restart path. |

## Issue #134 exact-fixture acceptance update, 2026-08-30

The 882-page exact lane passed 3/3 under a quiet-host one-minute load of 2.22.
The six native renderer save totals were 2451.7, 2946.0, 3034.7, 3171.3,
3382.9, and 4153.4 ms, all below the 8000 ms save SLO. Wrapper evidence is
`.devkit/analysis/gates/2026-08-30T01-04-14-976Z-3250624-e220a53e.ndjson`.

The 2646-page xlarge lane passed 2/2 under a quiet-host one-minute load of
2.87. The stage-aware heartbeat policy passed in the actual save window at
726.2 ms; the separate pre-save placement stall reached 9105.3 ms and is
tracked by #149. Render probes, visible interaction, live dirty state,
committed revisions, qpdf checks, hard restarts, and bounded structural
comparisons all passed. Wrapper evidence is
`.devkit/analysis/gates/2026-08-30T00-42-44-909Z-3212604-4553f90b.ndjson`.

Rows TEST-003, TEST-004, TEST-006, TEST-008, TEST-012, TEST-017, and TEST-027
are fixed/published for the exact-fixture acceptance. The issue is closed.
The pre-save placement stall is intentionally not hidden and remains open as
#149. CodeRabbit was attempted for the changed code and the later slices but
the included-review rate limit was exhausted; no paid review was requested.

## Large-document resource hotspots

| ID | Severity | Class | Finding | Evidence | Required red regression test or probe | Owner/status | Fix SHA | Gates | Remaining acceptance |
| --- | --- | --- | --- | --- | --- | --- | --- | --- | --- |
| HOT-001 | Medium | product | Native PDF preview has a windowed override but can fall back to `Array.from({length: pageCount})`, creating a page-count-proportional compatibility array. | `cap-audit.md:98-103` cites `nativePdfPreview.ts:160-189`. | Force the compatibility fallback on a high-page-count PDF and assert memory stays within the declared budget or the operation refuses explicitly. | fixed/published | `9fcc32c34` | Native preview boundary and compact lifecycle tests pass; focused native preview gates, typecheck, ESLint, and diff checks passed. | Complete. The verified fix is published on remote main; the >5,000-page branch stays compact and the 5,000-page compatibility boundary is explicit. |
| HOT-002 | Medium | product | Browser all-pages image export creates a dense page target list, and browser DjVu TIFF retains decoded pages in an array despite a 64 MiB decoded-image budget. | `cap-audit.md:104` cites `createBrowserImageExportCapability.ts:469-476,564-597`. | Run all-pages browser image and DjVu export on a high-page-count input; assert target and decoded-page memory remain bounded. | fixed/published | `ba2539b3d`, `d9aa75cf0` | Red-first all-pages target and 256-page decoded-retention regressions pass through both handle-backed and bounded-inline paths; repository typecheck, targeted ESLint, Fallow, and focused architecture checks pass. | Complete. Browser export refuses dense targets above 100,000 pages, and DjVu TIFF decodes and writes one page at a time. |
| HOT-003 | Medium | product | `streamingImagePdfWriter` builds a `/Kids` array for every output page, with no continuation inside the catalog object. | `cap-audit.md:105` cites `streamingImagePdfWriter.ts:285-295`. | Export a high-page-count image PDF under an allocation budget and assert catalog construction remains bounded or fails explicitly. | fixed/published | `a75a1036c`, `9468c36b3` | Red-first fanout, multi-level, and one-million-page planning regressions pass; repository typecheck, targeted ESLint, Fallow, and focused architecture checks pass. | Complete. The balanced page tree bounds every catalog node and keeps level descriptors logarithmic in output page count. |
| HOT-004 | Medium | product | Page layout builds dense width, height, and row-index arrays, though callers select sparse metrics above 100,000 pages. | `cap-audit.md:106` cites `buildPageLayoutMetrics.ts:421-433,605`. | Force the compatibility path near its threshold and the sparse path above it; assert no unexpected dense allocation crosses the budget. | viewer / implemented; acceptance open | `9fcc32c34`, `b4bafc271`, `875642009` | 100,000-page dense and 100,001-page sparse layout regressions pass with segmented thumbnail/page-source consumers. The published follow-ups keep zoom-anchor and sparse page-source layout projection lazy; focused page-source runtime tests cover a one-million-page lazy collection, and targeted ESLint, vue-tsc, and diff checks pass. | Live 138,000-page Electron memory and generic source-runtime residency evidence remains open under #131/#132. |

## GitHub issue map

Umbrella issue [#112](https://github.com/evb0110/evb-viewer/issues/112)
tracks the full audit. The 25 implementation issues below cover every open
ledger ID exactly once. The eleven `fixed/published` IDs remain checked in the
umbrella and are not reopened as implementation work.

| Issue | Ledger IDs |
| --- | --- |
| [#113 Preserve unopened PDF outlines through structural page operations](https://github.com/evb0110/evb-viewer/issues/113) | `PDF-001` |
| [#114 Preserve compact page labels through xlarge structural page operations](https://github.com/evb0110/evb-viewer/issues/114) | `PDF-002` |
| [#115 Import Text annotations and Popup notes into canonical state](https://github.com/evb0110/evb-viewer/issues/115) | `PDF-003` |
| [#116 Persist imported sticky-note geometry through native saves](https://github.com/evb0110/evb-viewer/issues/116) | `PDF-004` |
| [#117 Make browser recent-file pruning transactional with IndexedDB durability](https://github.com/evb0110/evb-viewer/issues/117) | `PDF-005` |
| [#118 Make native split, text-overlay, and conformance outputs alias-safe](https://github.com/evb0110/evb-viewer/issues/118) | `PDF-006` |
| [#119 Preserve outlines and page labels in browser page operations](https://github.com/evb0110/evb-viewer/issues/119) | `PDF-007` |
| [#120 Publish original-path saves with external-editor compare-and-swap safety](https://github.com/evb0110/evb-viewer/issues/120) | `SAV-008` |
| [#121 Enforce renderer ownership for app-temp reads](https://github.com/evb0110/evb-viewer/issues/121) | `SEC-001` |
| [#122 Make save, Save As, sidecars, journals, and queued mutations one transaction](https://github.com/evb0110/evb-viewer/issues/122) | `SAV-002`, `SAV-003`, `SAV-004`, `SAV-005`, `SAV-010`, `SAV-011` |
| [#123 Make large working-copy creation, cloning, and materialization revision-safe](https://github.com/evb0110/evb-viewer/issues/123) | `SAV-006`, `SAV-009`, `SAV-012`, `SEC-002`, `SEC-003` |
| [#124 Tie native utilities, queues, leases, handles, and cancellation to renderer lifecycle](https://github.com/evb0110/evb-viewer/issues/124) | `SAV-013`, `SAV-014`, `SAV-015`, `SAV-016`, `SAV-017`, `SAV-018`, `SAV-019` |
| [#125 Complete bounded native annotation projection and placed-image lifecycle](https://github.com/evb0110/evb-viewer/issues/125) | `ANN-001`, `ANN-002`, `ANN-003`, `ANN-004`, `ANN-005`, `CAP-001` |
| [#126 Make compact selections and xlarge page operations atomic and bounded](https://github.com/evb0110/evb-viewer/issues/126) | `SEL-001`, `SEL-002`, `SEL-003`, `SEL-004`, `SEL-005`, `SEL-006`, `SEL-007`, `CAP-002`, `CAP-003` |
| [#127 Stream PDF image export, TIFF assembly, and print planning](https://github.com/evb0110/evb-viewer/issues/127) | `EXP-001`, `EXP-002`, `EXP-003`, `EXP-005`, `EXP-006`, `HOT-002`, `HOT-003` |
| [#128 Remove browser and DjVu whole-document fallbacks from large-document paths](https://github.com/evb0110/evb-viewer/issues/128) | `EXP-004`, `EXP-007`, `EXP-008`, `EXP-009`, `HOT-001` |
| [#129 Make search sidecar completeness and query budgets fail closed](https://github.com/evb0110/evb-viewer/issues/129) | `SRCH-001`, `SRCH-002`, `SRCH-003` |
| [#130 Make OCR catalogs revision-safe, bounded, and corruption-aware](https://github.com/evb0110/evb-viewer/issues/130) | `SRCH-004`, `SRCH-005`, `SRCH-006`, `SRCH-007` |
| [#131 Keep scan-cleanup analysis and persistence bounded for xlarge documents](https://github.com/evb0110/evb-viewer/issues/131) | `SCAN-001`, `SCAN-002`, `SCAN-003`, `SCAN-004`, `SCAN-005`, `SCAN-006`, `SCAN-007` |
| [#132 Keep viewer thumbnails, outlines, preview, and page layout sparse at scale](https://github.com/evb0110/evb-viewer/issues/132) | `SCAN-008`, `SCAN-009`, `HOT-004` |
| [#133 Bound and validate native numeric, recursive, and sidecar inputs](https://github.com/evb0110/evb-viewer/issues/133) | `SEC-004`, `SEC-005` |
| [#134 Make large PDF acceptance prove real interaction, commits, rendering, and latency](https://github.com/evb0110/evb-viewer/issues/134) | `TEST-003`, `TEST-004`, `TEST-006`, `TEST-008`, `TEST-012`, `TEST-017`, `TEST-027` |
| [#135 Make Electron acceptance teardown, timeout, and test isolation fail closed](https://github.com/evb0110/evb-viewer/issues/135) | `TEST-007`, `TEST-013`, `TEST-016`, `TEST-020`, `TEST-026` |
| [#136 Add required exact-fixture and native-save CI coverage](https://github.com/evb0110/evb-viewer/issues/136) | `TEST-001`, `TEST-002`, `TEST-005`, `TEST-009`, `TEST-010`, `TEST-018`, `TEST-028` |
| [#138 Add aggregate sidecar and ink-stroke budgets to native mutation inputs](https://github.com/evb0110/evb-viewer/issues/138) | `SEC-006` |
| [#144 Prevent retired annotation refs from blocking Highlight and Ink saves](https://github.com/evb0110/evb-viewer/issues/144) | `ANN-006`, `ANN-007` |

Post-audit issues and follow-ups:

| Issue | Current state and ledger connection |
| --- | --- |
| [#139 Large-PDF FreeText annotations](https://github.com/evb0110/evb-viewer/issues/139) | `ANN-005` is complete for the persisted-sidebar-delete reproduction at `df72592c0`; `575d0e9a7` also admits persisted PDF.js `Typewriter` editors into the canonical store, `f992d0654` bounds page-local FreeText resize-history lookup, and `4f89f9a11` with follow-up `029e84c4f` keep painted FreeText visible through the exact Mac save/layout-transition and hard-reopen case. `73fdb3848` normalizes PDF.js object-reference generations during legacy projection and prevents duplicate canonical rows; its focused annotation/save gate passed 159/159, repository typecheck passed, and the exact Mac 882-page regression passed 1/1 with qpdf checks and hard restart. Evidence: `.devkit/analysis/gates/2026-08-30T13-22-27-170Z-20230-d585d4d6.ndjson`. The follow-up uses the exact local 882-page fixture, qpdf-checks all five saved `/FreeText` objects, and confirms five sidebar entries after reopen. The user recording `CleanShot 2026-08-29 at 11.30.45.mp4` shows two existing Inline Note texts still painted after the sidebar reaches zero annotations, so the broader close-transition and multi-existing-editor acceptance remains open. |
| [#145 macOS print handoff](https://github.com/evb0110/evb-viewer/issues/145) | `17d61287e` waits for the hidden PDF-plugin surface, and `46cdef1b5` keeps that compositor-visible surface fully transparent through the settle delay and restores opacity after teardown. The 18-test print unit suite plus Electron/tests typechecks, ESLint, and diff check pass. A real macOS system-sheet and nonblank-output run remains open. |
| [#146 Save transaction follow-ups](https://github.com/evb0110/evb-viewer/issues/146) | Closed. `SAV-020` records the completed transaction policy: journaled persistence, fenced rollback and recovery, symlink refusal, destination-scoped backup cleanup, and bounded Windows content fingerprints. `c6c5fd44b` adds the final POSIX real-filesystem regressions. |
| [#149 Xlarge pre-save renderer stall](https://github.com/evb0110/evb-viewer/issues/149) | `d0ab3779c` bounds save-time editor-page discovery, and `45ed63210` restores the all-stage heartbeat assertion. The exact quiet-host 2646-page lane passed 2/2 with placement and save gaps below 5 s. Verified and closed. |

### TEST-012 / TEST-017 follow-up for #149, 2026-08-30

The earlier instrumented exact 2,646-page rerun used owner commit `acf4c1505`
and local bounded-call-site commit `3c969013a`. The pre-save FreeText placement
window still failed the restored all-stage heartbeat assertion at 11,569.0 ms
for `setInterval` and 12,146.2 ms for `MessageChannel`; the Worker gap was
277.9 ms. The renderer probe saw 13 mounted annotation/editor layers, 79,469
DOM mutations, and a maximum requestAnimationFrame gap of 766.6 ms. The save
window measured 881.2 ms, 896.2 ms, and 257.2 ms for those same three probes.

The earlier 11,569.0 ms and 12,146.2 ms pre-save gaps came from the
page-count-sized `collectEditorCommentSummaries` path in #139. After
`d0ab3779c` bounded that scan to materialized annotation pages, the quiet-host
exact 2,646-page lane passed 2/2 with the all-stage assertion enabled. Session B
pre-save measured 1,941.5 ms (`setInterval`) and 2,048.1 ms (`MessageChannel`),
with a 110.1 ms Worker gap. The save stage measured 254.7 ms, 342.2 ms, and
110 ms. Every heartbeat had a valid MessageChannel sample, and the renderer
placement probe recorded 77,065 DOM mutations with a 183.3 ms maximum frame
gap. The fixture was 2,168,527,413 bytes and 2,646 pages, staged through the
bounded stream path.

Final telemetry:
`.devkit/test/electron-e2e-artifacts/xlarge-document-acceptance-d0ab3779c-full-heartbeat.json`.
Wrapper evidence:
`.devkit/analysis/gates/2026-08-30T03-44-19-773Z-72471-83525993.ndjson`.
The placement and save heartbeat requirements are enforced by `45ed63210`,
so #149 is verified and closed. CodeRabbit's final pass found no remaining
issue.
| #142, #147, and #148 | Closed after their published source fixes and focused or cross-process acceptance. #142 is `0ef7011d2`, #147 is `85670d391`, and #148 is `42601baf0`. |

Fixed and verified in the umbrella: `SAV-001`, `SAV-007`, `TEST-011`,
`TEST-014`, `TEST-015`, `TEST-019`, `TEST-021`, `TEST-022`, `TEST-023`,
`TEST-024`, and `TEST-025`.

## Large-save regression acceptance checklist

This checklist matches the next acceptance lane required by the exhaustive
report at `full-audit.md:130`.

- [x] Fail closed unless the fixture SHA, byte size, and qpdf page count match.
- [x] Start a fresh Electron process and use visible controls with real pointer
  and keyboard input.
- [x] Prove live PDF.js annotation storage and the intended dirty state.
- [x] Wait for each exact `save-committed` path and revision.
- [x] Verify the target hash, `qpdf --check`, Contents, NM, AP, Rect, and Popup
  parent through bounded independent probes.
- [x] Kill and hard-restart Electron after the first save.
- [x] Restore or reopen through the genuine product path and prove clean
  hydration.
- [x] Make a second real annotation edit and save it through the native path.
- [x] Hard-restart a second time and prove both commits.
- [x] Require the runtime IPC probe.
- [x] Fail on helper fallback, missing probe, teardown error, or surviving
  Electron, Nuxt, qpdf, utility, lease, or CDP work.
- [x] Pass the exact macOS and Linux fixture in hidden mode on the same final
  code commit. Earlier visible or no-sandbox runs remain diagnostic only, per
  the final user instruction that all current-commit verification be headless.
- [x] Pass focused tests, the full unit gate, lint, typecheck, CodeRabbit, and
  independent spec and standards review on the final reviewed code commit.
- [x] Publish the final reviewed and re-verified code commit to remote `main`
  and record its ancestry.

The Linux report's lower-cost red-test order is retained as implementation
guidance: a committed-event blocking smoke, a small-fixture hard restart with
a second save and reopen, a qpdf structural check after each save, and the
opt-in exact-fixture lane with SHA admission and the same hard-restart sequence
(`linux-flow-audit.md:238-247`).

## Boundaries inspected and not counted as defects

The reports also record bounded paths and rejected hypotheses. They are not
ledger items because the reports found no silent truncation or unsafe behavior
in those paths at the audited baseline.

| Boundary | Audit conclusion | Evidence |
| --- | --- | --- |
| Native eager PDF and qpdf structure | Eager and retained-structure caps are safety limits; path-backed PDFs use qpdf above the eager threshold without a whole-document source allocation. | `cap-audit.md:169-182`; `native-crosscut-audit.md:128-134` |
| Native offsets and staged output | Checked `u64` offsets, xref streams, staged output, and mutation receipt checks passed their reported tests. The separate JavaScript publication race remains `SAV-008`. | `native-crosscut-audit.md:120-145`; `cap-audit.md:192-203` |
| Lazy materialization | Chunked copy, abort checks, source and target hashes, and the registration fence form a continuation path. The missing renderer teardown hook remains `SAV-015`. | `native-crosscut-audit.md:146-149` |
| OCR, search, scan cleanup, DjVu, image, and TIFF continuation paths | The native and sidecar paths have bounded windows, shards, batches, or explicit format limits. Their separate defects are listed above where the reports found them. | `native-crosscut-audit.md:150-163` |
| Browser platform routing | Electron routing requires the bridge, desktop runtime, or explicit Electron route; an Electron-shaped user agent alone does not select missing IPC. | `native-crosscut-audit.md:164-167` |
| Page identity, metrics, and compatibility labels | Sparse identity and metrics paths are bounded; the label compatibility null-to-empty rewrite is the separate `PDF-002` defect. | `cap-audit.md:100-106` |
